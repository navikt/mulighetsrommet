name: Check OpenAPI Specs

on:
  push:
    paths:
      - .github/workflows/check-openapi-specs.yaml
      - mulighetsrommet-api/server/src/**

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CI: true
  TZ: Europe/Amsterdam

jobs:
  ci:
    name: Check OpenAPI Spec Generation
    runs-on:
      labels: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Setup backend
        uses: ./.github/actions/setup-backend

      - name: Generate OpenAPI Specs
        run: ./gradlew :mulighetsrommet-api:server:generateOpenApi

      - name: Check for uncommitted changes
        run: |
          if [ -n "$(git status --porcelain)" ]; then
            echo "::error::OpenAPI specs are not up to date! Please run './gradlew :mulighetsrommet-api:server:generateOpenApi' locally and commit the changes."
            echo "The following files need to be committed:"
            git status --porcelain
            exit 1
          else
            echo "✅ OpenAPI specs are up to date."
          fi
