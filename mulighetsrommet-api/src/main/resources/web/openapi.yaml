openapi: 3.1.0

info:
  version: 1.0.0
  title: Mulighetsrommet API

security:
  - bearerAuth: []

paths:
  /api/v1/intern/bruker:
    post:
      tags:
        - Bruker
      operationId: getBrukerdata
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GetBrukerRequest"
        required: true
      responses:
        200:
          description: Data om bruker
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Bruker"
        403:
          description: Mangler tilganger til å hente data om bruker
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string

  /api/v1/intern/avtaler/{id}:
    parameters:
      - $ref: "#/components/parameters/ID"
    get:
      tags:
        - Avtaler
      operationId: getAvtale
      responses:
        200:
          description: The specified avtale.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AvtaleDto"
        404:
          description: "Avtalen ble ikke funnet"
          content:
            application/json:
              schema:
                type: string

  /api/v1/intern/avtaler/{id}/avbryt:
    parameters:
      - $ref: "#/components/parameters/ID"
    put:
      tags:
        - Avtaler
      operationId: avbrytAvtale
      requestBody:
        content:
          application/json:
            schema:
              oneOf:
                - $ref: "#/components/schemas/AvbrytAvtaleRequest"
                - $ref: "#/components/schemas/CustomAvbrytAvtaleRequest"
      responses:
        200:
          description: The specified avtale you want to 'Avbryte'
        404:
          description: "Avtalen ble ikke funnet og kunne ikke avbrytes"
          content:
            application/json:
              schema:
                type: string

  /api/v1/intern/avtaler:
    get:
      tags:
        - Avtaler
      operationId: getAvtaler
      parameters:
        - in: query
          name: tiltakstyper
          schema:
            type: array
            items:
              type: string
          description: ID for tiltakstyper om du ønsker avtaler for en spesifikk tiltakstype
        - in: query
          name: search
          schema:
            type: string
          description: Search for avtaler by the name
        - in: query
          name: statuser
          schema:
            type: array
            items:
              $ref: "#/components/schemas/Avtalestatus"
          description: Statuser for avtale
        - in: query
          name: avtaletyper
          schema:
            type: array
            items:
              $ref: "#/components/schemas/Avtaletype"
          description: Avtaletyper
        - in: query
          name: navRegioner
          schema:
            type: array
            items:
              type: string
          description: 4 digit enhetskode
        - in: query
          name: arrangorer
          schema:
            type: array
            items:
              type: string
              format: uuid
          description: Id til arrangører for avtaler
        - in: query
          name: page
          schema:
            type: number
          description: Page number, default 1
        - in: query
          name: size
          schema:
            type: number
          description: Page size, default 50
        - in: query
          name: sort
          schema:
            $ref: "#/components/schemas/SorteringAvtaler"
        - in: query
          name: personvernBekreftet
          schema:
            type: boolean
      responses:
        200:
          description: Object of paginated avtaler connected to specified tiltakstype-id.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginertAvtale"
    put:
      tags:
        - Avtaler
      operationId: upsertAvtale
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AvtaleRequest"
      responses:
        200:
          description: Avtale response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AvtaleDto"
        400:
          description: Respons med valideringsfeil
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationError"
        500:
          description: "Kunne ikke opprette avtale"
          content:
            application/json:
              schema:
                type: string

  /api/v1/intern/avtaler/mine:
    get:
      tags:
        - Avtaler
      operationId: getMineAvtaler
      parameters:
        - in: query
          name: tiltakstyper
          schema:
            type: array
            items:
              type: string
          description: ID for tiltakstyper om du ønsker avtaler for en spesifikk tiltakstype
        - in: query
          name: search
          schema:
            type: string
          description: Search for avtaler by the name
        - in: query
          name: page
          schema:
            type: number
          description: Page number, default 1
        - in: query
          name: size
          schema:
            type: number
          description: Page size, default 50
        - in: query
          name: sort
          schema:
            $ref: "#/components/schemas/SorteringAvtaler"
        - in: query
          name: statuser
          schema:
            type: array
            items:
              $ref: "#/components/schemas/Avtalestatus"
          description: Statuser for avtale
        - in: query
          name: navRegioner
          schema:
            type: array
            items:
              type: string
          description: 4 digit enhetskode
        - in: query
          name: arrangorer
          schema:
            type: array
            items:
              type: string
              format: uuid
          description: Id til arrangør for avtaler
      responses:
        200:
          description: Object of paginated avtaler connected to specified tiltakstype-id where logged in user is responsible for avtale.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginertAvtale"

  /api/v1/intern/avtaler/excel:
    get:
      tags:
        - Avtaler
      operationId: lastNedAvtalerSomExcel
      parameters:
        - in: query
          name: tiltakstyper
          schema:
            type: array
            items:
              type: string
          description: ID for tiltakstyper om du ønsker avtaler for en spesifikk tiltakstype
        - in: query
          name: search
          schema:
            type: string
          description: Search for avtaler by the name
        - in: query
          name: statuser
          schema:
            type: array
            items:
              $ref: "#/components/schemas/Avtalestatus"
          description: Statuser for avtale
        - in: query
          name: avtaletyper
          schema:
            type: array
            items:
              $ref: "#/components/schemas/Avtaletype"
          description: Avtaletyper
        - in: query
          name: navRegioner
          schema:
            type: array
            items:
              type: string
          description: 4 digit enhetskode
        - in: query
          name: arrangorer
          schema:
            type: array
            items:
              type: string
              format: uuid
          description: Id til arrangører for avtaler
        - in: query
          name: page
          schema:
            type: number
          description: Page number, default 1
        - in: query
          name: size
          schema:
            type: number
          description: Page size, default 50
        - in: query
          name: sort
          schema:
            $ref: "#/components/schemas/SorteringAvtaler"
        - in: query
          name: personvernBekreftet
          schema:
            type: boolean
        - in: query
          name: visMineAvtaler
          schema:
            type: boolean
      responses:
        200:
          description: Excel-fil med avtaler
          content:
            application/vnd.openxmlformats-officedocument.spreadsheetml.sheet:
              schema:
                type: string
                format: binary

  /api/v1/intern/avtaler/kontaktperson:
    delete:
      tags:
        - Avtaler
      operationId: frikobleKontaktpersonFraAvtale
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FrikobleKontaktpersonRequest"
      responses:
        200:
          description: OK hvis frikobling gikk greit
          content:
            application/json:
              schema:
                type: string

  /api/v1/intern/avtaler/{id}/historikk:
    get:
      tags:
        - Avtaler
      operationId: getAvtaleEndringshistorikk
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: ID for avtalen
      responses:
        200:
          description: Endringshistorikken for avtalen
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Endringshistorikk"

  /api/v1/intern/janzz/sertifiseringer/sok:
    get:
      tags:
        - sertifiseringer
        - janzz
      operationId: sokSertifiseringer
      parameters:
        - in: query
          name: q
          required: true
          schema:
            type: string
          description: search query
      responses:
        200:
          description: sertifiseringer etter søk
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Sertifisering"

  /api/v1/intern/gjennomforinger/{id}/historikk:
    get:
      tags:
        - Gjennomforinger
      operationId: getGjennomforingEndringshistorikk
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: ID for gjennomføringen
      responses:
        200:
          description: Endringshistorikken for gjennomføringen
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Endringshistorikk"

  /api/v1/intern/gjennomforinger/{id}/deltaker-summary:
    get:
      tags:
        - Gjennomforinger
      operationId: getGjennomforingDeltakerSummary
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: ID for gjennomføringen
      responses:
        200:
          description: Oppsummert informasjon om gjennomførings deltakere
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GjennomforingDeltakerSummary"

  /api/v1/intern/gjennomforinger:
    get:
      tags:
        - Gjennomforinger
      operationId: getGjennomforinger
      parameters:
        - in: query
          name: page
          schema:
            type: number
          description: Page number, default 1
        - in: query
          name: search
          schema:
            type: string
          description: Search for gjennomføringer by the name
        - in: query
          name: navEnheter
          schema:
            type: array
            items:
              type: string
          description: 4 digit enhetskode
        - in: query
          name: tiltakstyper
          schema:
            type: array
            items:
              type: string
              format: uuid
          description: Id for tiltakstype knyttet til gjennomføringer
        - in: query
          name: statuser
          schema:
            type: array
            items:
              $ref: "#/components/schemas/GjennomforingStatus"
          description: Status for gjennomføring
        - in: query
          name: navRegioner
          schema:
            type: array
            items:
              type: string
        - in: query
          name: arrangorer
          schema:
            type: array
            items:
              type: string
              format: uuid
        - in: query
          name: publisert
          schema:
            type: [boolean, "null"]
        - in: query
          name: size
          schema:
            type: number
          description: Page size, default 50
        - in: query
          name: sort
          schema:
            $ref: "#/components/schemas/SorteringGjennomforinger"
        - in: query
          name: avtaleId
          schema:
            type: string
      responses:
        200:
          description: Object of paginated gjennomføringer.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginertGjennomforing"
    put:
      tags:
        - Gjennomforinger
      operationId: upsertGjennomforing
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GjennomforingRequest"
      responses:
        200:
          description: The specified gjennomføring with data about tiltakstype.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GjennomforingDto"
        400:
          description: Respons med valideringsfeil
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationError"
        500:
          description: "Kunne ikke opprette gjennomforing"
          content:
            application/json:
              schema:
                type: string

  /api/v1/intern/gjennomforinger/mine:
    get:
      tags:
        - Gjennomforinger
      operationId: getMineGjennomforinger
      parameters:
        - in: query
          name: navEnheter
          schema:
            type: array
            items:
              type: string
          description: 4 digit enhetskode
        - in: query
          name: tiltakstyper
          schema:
            type: array
            items:
              type: string
              format: uuid
          description: Id for tiltakstype knyttet til gjennomføringer
        - in: query
          name: statuser
          schema:
            type: array
            items:
              $ref: "#/components/schemas/GjennomforingStatus"
          description: Status for gjennomføring
        - in: query
          name: navRegioner
          schema:
            type: array
            items:
              type: string
        - in: query
          name: arrangorer
          schema:
            type: array
            items:
              type: string
              format: uuid
        - in: query
          name: page
          schema:
            type: number
          description: Page number, default 1
        - in: query
          name: search
          schema:
            type: string
          description: Search for gjennomføringer by the name
        - in: query
          name: size
          schema:
            type: number
          description: Page size, default 50
        - in: query
          name: sort
          schema:
            $ref: "#/components/schemas/SorteringGjennomforinger"
        - in: query
          name: avtaleId
          schema:
            type: string
      responses:
        200:
          description: Object of paginated gjennomføringer where logged in user is responsible for gjennomføringen.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginertGjennomforing"

  /api/v1/intern/gjennomforinger/excel:
    get:
      tags:
        - Gjennomforinger
      operationId: lastNedGjennomforingerSomExcel
      parameters:
        - in: query
          name: page
          schema:
            type: number
          description: Page number, default 1
        - in: query
          name: search
          schema:
            type: string
          description: Search for gjennomføringer by the name
        - in: query
          name: navEnheter
          schema:
            type: array
            items:
              type: string
          description: 4 digit enhetskode
        - in: query
          name: tiltakstyper
          schema:
            type: array
            items:
              type: string
              format: uuid
          description: Id for tiltakstype knyttet til gjennomføringer
        - in: query
          name: statuser
          schema:
            type: array
            items:
              $ref: "#/components/schemas/GjennomforingStatus"
          description: Status for gjennomføring
        - in: query
          name: navRegioner
          schema:
            type: array
            items:
              type: string
        - in: query
          name: arrangorer
          schema:
            type: array
            items:
              type: string
              format: uuid
        - in: query
          name: publisert
          schema:
            type: [boolean, "null"]
        - in: query
          name: size
          schema:
            type: number
          description: Page size, default 50
        - in: query
          name: sort
          schema:
            $ref: "#/components/schemas/SorteringGjennomforinger"
        - in: query
          name: avtaleId
          schema:
            type: string
        - in: query
          name: visMineTiltaksgjennomforinger
          schema:
            type: boolean
      responses:
        200:
          description: Excel-fil med gjennomforinger
          content:
            application/vnd.openxmlformats-officedocument.spreadsheetml.sheet:
              schema:
                type: string
                format: binary

  /api/v1/intern/gjennomforinger/{id}:
    parameters:
      - $ref: "#/components/parameters/ID"
    get:
      tags:
        - Gjennomforinger
      operationId: getGjennomforing
      responses:
        200:
          description: The specified gjennomføring with data about tiltakstype.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GjennomforingDto"
        404:
          description: "Gjennomføringen ble ikke funnet"
          content:
            application/json:
              schema:
                type: string

  /api/v1/intern/gjennomforinger/{id}/tiltaksnummer:
    parameters:
      - $ref: "#/components/parameters/ID"
    get:
      tags:
        - Gjennomforinger
      operationId: getTiltaksnummer
      responses:
        200:
          description: Tiltaksnummer til gjennomføringen.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TiltaksnummerResponse"
        204:
          description: Gjennomføringen har enda ikke tiltaksnummer.
        404:
          description: Gjennomføringen ble ikke funnet.
          content:
            application/json:
              schema:
                type: string

  /api/v1/intern/gjennomforinger/{id}/avtale:
    parameters:
      - $ref: "#/components/parameters/ID"
    put:
      tags:
        - Gjennomforinger
      operationId: setAvtaleForGjennomforing
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SetAvtaleForGjennomforingRequest"
      responses:
        200:
          description: "Gjennomføring ble oppdatert med avtale"

  /api/v1/intern/gjennomforinger/{id}/avbryt:
    parameters:
      - $ref: "#/components/parameters/ID"
    put:
      tags:
        - Gjennomforinger
      operationId: avbrytGjennomforing
      requestBody:
        content:
          application/json:
            schema:
              oneOf:
                - $ref: "#/components/schemas/AvbrytGjennomforingRequest"
                - $ref: "#/components/schemas/CustomAvbrytGjennomforingRequest"
      responses:
        200:
          description: The specified gjennomføring you want to 'Avbryte'
          content:
            application/json:
              schema:
                type: string
        404:
          description: "Mangler eller ugyldig id"
          content:
            application/json:
              schema:
                type: string

  /api/v1/intern/gjennomforinger/{id}/tilgjengelig-for-veileder:
    parameters:
      - $ref: "#/components/parameters/ID"
    put:
      tags:
        - Gjennomforinger
      operationId: setPublisert
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PublisertRequest"
      responses:
        200:
          description: Update for 'tilgjengelig for veileder' was successful

  /api/v1/intern/gjennomforinger/{id}/apent-for-pamelding:
    parameters:
      - $ref: "#/components/parameters/ID"
    put:
      tags:
        - Gjennomforinger
      operationId: setApentForPamelding
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SetApentForPameldingRequest"
      responses:
        200:
          description: OK

  /api/v1/intern/gjennomforinger/{id}/tilgjengelig-for-arrangor:
    parameters:
      - $ref: "#/components/parameters/ID"
    put:
      tags:
        - Gjennomforinger
      operationId: setTilgjengeligForArrangor
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SetTilgjengligForArrangorRequest"
      responses:
        200:
          description: OK

  /api/v1/intern/gjennomforinger/{id}/stengt-hos-arrangor:
    parameters:
      - $ref: "#/components/parameters/ID"
    put:
      tags:
        - Gjennomforinger
      operationId: setStengtHosArrangor
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SetStengtHosArrangorRequest"
      responses:
        200:
          description: OK
        400:
          description: Respons med valideringsfeil
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationError"

  /api/v1/intern/gjennomforinger/{id}/stengt-hos-arrangor/{periodeId}:
    parameters:
      - $ref: "#/components/parameters/ID"
      - name: periodeId
        in: path
        description: Id til periode som skal slettes
        required: true
        schema:
          type: number
    delete:
      tags:
        - Gjennomforinger
      operationId: deleteStengtHosArrangor
      responses:
        200:
          description: OK

  /api/v1/intern/gjennomforinger/kontaktperson:
    delete:
      tags:
        - Gjennomforinger
      operationId: frikobleKontaktpersonFraGjennomforing
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FrikobleKontaktpersonRequest"
      responses:
        200:
          description: OK hvis frikobling gikk greit
          content:
            application/json:
              schema:
                type: string

  /api/v1/intern/tiltakstyper:
    get:
      tags:
        - Tiltakstyper
      operationId: getTiltakstyper
      parameters:
        - in: query
          name: search
          schema:
            type: string
          description: Search for tiltakstyper
        - in: query
          name: tiltakstypestatuser
          schema:
            type: array
            items:
              $ref: "#/components/schemas/TiltakstypeStatus"
          description: Statuser for tiltakstyper
        - in: query
          name: page
          schema:
            type: number
          description: Page number, default 1
        - in: query
          name: size
          schema:
            type: number
          description: Page size, default 50
        - in: query
          name: sort
          schema:
            $ref: "#/components/schemas/SorteringTiltakstyper"
      responses:
        200:
          description: Object of paginerte tiltakstyper.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginertTiltakstype"

  /api/v1/intern/tiltakstyper/{id}:
    get:
      tags:
        - Tiltakstyper
      operationId: getTiltakstypeById
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        200:
          description: the specified tiltakstype
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TiltakstypeDto"
        404:
          description: "Tiltakstypen ble ikke funnet"
          content:
            text/plain:
              schema:
                type: string

  /api/v1/intern/tiltakstyper/{id}/faneinnhold:
    get:
      tags:
        - Tiltakstyper
      operationId: getTiltakstypeFaneinnhold
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        200:
          description: Faneinnhold for the specified tiltakstype
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VeilederflateTiltakstype"
        404:
          description: "Faneinnholdet for tiltakstypen ble ikke funnet"
          content:
            text/plain:
              schema:
                type: string

  /api/v1/intern/veileder/innsatsgrupper:
    get:
      tags:
        - VeilederTiltak
      operationId: getInnsatsgrupper
      responses:
        200:
          description: Hent innsatsgrupper
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/VeilederflateInnsatsgruppe"

  /api/v1/intern/veileder/tiltakstyper:
    get:
      tags:
        - VeilederTiltak
      operationId: getVeilederflateTiltakstyper
      responses:
        200:
          description: Hent tiltakstyper
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/VeilederflateTiltakstype"

  /api/v1/intern/veileder/gjennomforinger:
    get:
      tags:
        - VeilederTiltak
      operationId: getAllVeilederTiltak
      parameters:
        - $ref: "#/components/parameters/SearchParam"
        - $ref: "#/components/parameters/ApentForPameldingParam"
        - $ref: "#/components/parameters/InnsatsgruppeParam"
        - $ref: "#/components/parameters/EnheterParam"
        - $ref: "#/components/parameters/TiltakstyperParam"
        - $ref: "#/components/parameters/ErSykmeldtMedArbeidsgiverParam"
      responses:
        200:
          description: Hent tiltak
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/VeilederflateTiltak"

  /api/v1/intern/veileder/gjennomforinger/{id}:
    get:
      tags:
        - VeilederTiltak
      operationId: getVeilederTiltak
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        200:
          description: Hent tiltak for id
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VeilederflateTiltak"

  /api/v1/intern/veileder/preview/gjennomforinger:
    get:
      tags:
        - VeilederTiltak
      operationId: getAllPreviewTiltak
      parameters:
        - $ref: "#/components/parameters/SearchParam"
        - $ref: "#/components/parameters/ApentForPameldingParam"
        - $ref: "#/components/parameters/InnsatsgruppeParam"
        - $ref: "#/components/parameters/EnheterParam"
        - $ref: "#/components/parameters/TiltakstyperParam"
      responses:
        200:
          description: Hent preview tiltak
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/VeilederflateTiltak"

  /api/v1/intern/veileder/preview/gjennomforinger/{id}:
    get:
      tags:
        - VeilederTiltak
      operationId: getPreviewTiltak
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        200:
          description: Hent preview tiltak for id
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VeilederflateTiltak"

  /api/v1/intern/veileder/nav/gjennomforinger:
    get:
      tags:
        - VeilederTiltak
      operationId: getAllNavTiltak
      parameters:
        - $ref: "#/components/parameters/SearchParam"
        - $ref: "#/components/parameters/ApentForPameldingParam"
        - $ref: "#/components/parameters/InnsatsgruppeParam"
        - $ref: "#/components/parameters/EnheterParam"
        - $ref: "#/components/parameters/TiltakstyperParam"
      responses:
        200:
          description: Hent tiltak
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/VeilederflateTiltak"

  /api/v1/intern/veileder/nav/gjennomforinger/{id}:
    get:
      tags:
        - VeilederTiltak
      operationId: getNavTiltak
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        200:
          description: Hent tiltak for id
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VeilederflateTiltak"

  /api/v1/intern/bruker/tiltakshistorikk:
    post:
      tags:
        - Historikk
      operationId: getTiltakshistorikk
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GetDeltakelserForBrukerRequest"
        required: true
      responses:
        200:
          description: Brukerens deltakelser på arbeidsmarkedstiltak
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetDeltakelserForBrukerResponse"

  /api/v1/intern/bruker/deltakelse:
    post:
      tags:
        - Historikk
      operationId: hentDeltakelse
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GetAktivDeltakelseForBrukerRequest"
        required: true
      responses:
        200:
          description: Aktiv deltakelse
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeltakelseGruppetiltak"
        204:
          description: Fant ikke deltakelse for tiltak

  /api/v1/intern/ansatt:
    get:
      tags:
        - Ansatt
      operationId: hentAnsatte
      parameters:
        - in: query
          name: roller
          schema:
            type: array
            items:
              $ref: "#/components/schemas/Rolle"
          description: Nav-Ansatt rollen du ønsker å filtrere på
      responses:
        200:
          description: Get list of employeees
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/NavAnsatt"

  /api/v1/intern/veileder/me:
    get:
      tags:
        - Veileder
      operationId: getVeileder
      responses:
        200:
          description: Information about NavVeileder
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NavVeileder"

  /api/v1/intern/ansatt/me:
    get:
      tags:
        - Ansatt
      operationId: hentInfoOmAnsatt
      responses:
        200:
          description: Information about employee
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NavAnsatt"
        404:
          description: "Fant ingen data om 'ansatt'"
          content:
            application/json:
              schema:
                type: string

  /api/v1/intern/ansatt/sok:
    get:
      tags:
        - Ansatt
      operationId: sokAnsatt
      parameters:
        - in: query
          name: q
          required: true
          schema:
            type: string
          description: search query
      responses:
        200:
          description: Information about employee
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/NavAnsatt"

  /api/v1/intern/del-med-bruker:
    post:
      tags:
        - Del med bruker
      operationId: delTiltakMedBruker
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DelTiltakMedBrukerRequest"
      responses:
        200:
          description: Tiltak har blitt delt med bruker
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DelTiltakMedBrukerResponse"
        409:
          description: Tiltak ble ikke delt med bruker
          content:
            application/json:
              schema:
                type: string

  /api/v1/intern/del-med-bruker/status:
    post:
      tags:
        - Del med bruker
      operationId: getDelMedBruker
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GetDelMedBrukerRequest"
        required: true
      responses:
        200:
          description: "Last entry in database about share with user"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DelMedBruker"
        204:
          description: "No content found"

  /api/v1/intern/del-med-bruker/alle:
    post:
      tags:
        - Del med bruker
      operationId: getAlleTiltakDeltMedBruker
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GetAlleDeltMedBrukerRequest"
        required: true
      responses:
        200:
          description: "Latest entries in database about share with user for alle shared 'tiltak'"
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/DelMedBruker"
        204:
          description: "No content found"

  /api/v1/intern/del-med-bruker/historikk:
    post:
      tags:
        - Del med bruker
      operationId: getHistorikkForDeltMedBruker
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GetAlleDeltMedBrukerRequest"
        required: true
      responses:
        200:
          description: "Alle tiltak delt med bruker"
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TiltakDeltMedBruker"
        204:
          description: "No content found"

  /api/v1/intern/nav-enheter:
    get:
      tags:
        - NavEnheter
      operationId: getEnheter
      parameters:
        - in: query
          name: statuser
          schema:
            type: array
            items:
              $ref: "#/components/schemas/NavEnhetStatus"
          description: Hvilke status skal enheten ha
        - in: query
          name: typer
          schema:
            type: array
            items:
              $ref: "#/components/schemas/NavEnhetType"
          description: Hvilke typer enheter man spør på
      responses:
        200:
          description: Alle enheter filterert på filter
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/NavEnhet"

  /api/v1/intern/nav-enheter/regioner:
    get:
      tags:
        - NavEnheter
      operationId: getRegioner
      responses:
        200:
          description: Liste over regioner med underenheter
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/NavRegion"

  /api/v1/intern/nav-enheter/kostnadssted:
    get:
      tags:
        - NavEnheter
      operationId: getKostnadssted
      parameters:
        - in: query
          name: regioner
          schema:
            type: array
            items:
              type: string
          description: Hvilke regioner man henter for
      responses:
        200:
          description: Alle kostnadssteder filterert på filter
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/NavEnhet"

  /api/v1/intern/nav-enheter/{enhetsnummer}/overordnet:
    parameters:
      - in: "path"
        name: enhetsnummer
        required: true
        description: "Enhetsnummer for Nav-enhet"
        schema:
          type: string
    get:
      tags:
        - NavEnheter
      operationId: getOverordnetEnhet
      responses:
        200:
          description: Overordnet enhet for enhetsnummer
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NavEnhet"

  /api/v1/intern/arrangorer:
    get:
      tags:
        - Arrangor
      operationId: getArrangorer
      parameters:
        - in: query
          name: page
          schema:
            type: number
          description: Page number, default 1
        - in: query
          name: size
          schema:
            type: number
          description: Page size, default 50
        - in: query
          name: til
          schema:
            $ref: "#/components/schemas/ArrangorTil"
          description: Hent arrangører tilhørende avtaler eller gjennomforinger
        - in: query
          name: sok
          schema:
            type: string
          description: Søk i fritekst etter arrangør
        - in: query
          name: sortering
          schema:
            $ref: "#/components/schemas/SorteringArrangorer"
      responses:
        200:
          description: Paginert liste med virksomheter
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginertArrangor"

  /api/v1/intern/arrangorer/{id}:
    get:
      tags:
        - Arrangor
      operationId: getArrangorById
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        200:
          description: Arrangor for id
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Arrangor"

  /api/v1/intern/arrangorer/hovedenhet/{id}:
    get:
      tags:
        - Arrangor
      operationId: getArrangorHovedenhetById
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        200:
          description: Hovedenhet for arrangør for id
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Arrangor"

  /api/v1/intern/arrangorer/{orgnr}:
    post:
      tags:
        - Arrangor
      operationId: syncArrangorFromBrreg
      parameters:
        - in: path
          name: orgnr
          required: true
          schema:
            type: string
          description: 9-sifret organisasjonsnummer
      responses:
        200:
          description: Arrangor som matcher organisasjonsnummeret
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Arrangor"


  /api/v1/intern/arrangorer/{orgnr}/kontonummer:
    get:
      tags:
        - Arrangor
      operationId: getKontonummerForArrangor
      parameters:
        - in: path
          name: orgnr
          required: true
          schema:
            type: string
          description: 9-sifret organisasjonsnummer
      responses:
        200:
          description: Kontonummer for arrangør
          content:
            text/plain:
              schema:
                type: string

  /api/v1/intern/arrangorer/{id}/kontaktpersoner:
    get:
      tags:
        - Arrangor
      operationId: getArrangorKontaktpersoner
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        200:
          description: Kontaktpersoner lagret på arrangøren
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ArrangorKontaktperson"
    put:
      tags:
        - Arrangor
      operationId: upsertArrangorKontaktperson
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ArrangorKontaktpersonRequest"
      responses:
        200:
          description: Opprettet kontaktperson
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ArrangorKontaktperson"

  /api/v1/intern/arrangorer/kontaktperson/{id}:
    get:
      tags:
        - Arrangor
      operationId: getKoblingerTilDokumenter
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
          description: uuid til kontaktpersonen
      responses:
        200:
          description: Koblinger til gjennomføringer og avtaler for kontaktperson
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KoblingerForKontaktperson"

    delete:
      tags:
        - Arrangor
      operationId: deleteArrangorKontaktperson
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
          description: uuid til kontaktpersonen
      responses:
        200:
          description: Om kontaktpersonen ble slettet eller ikke
        500:
          description: Hvis noe gikk galt ved sletting av kontaktperson

  /api/v1/intern/brreg/sok:
    get:
      tags:
        - Brreg
      operationId: sokBrregHovedenhet
      parameters:
        - in: query
          name: sok
          required: true
          schema:
            type: string
          description: Søkestreng for navn på hovedenheter fra brreg
      responses:
        200:
          description: Liste med hovedenheter
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/BrregVirksomhet"

  /api/v1/intern/brreg/{orgnr}/underenheter:
    get:
      tags:
        - Brreg
      operationId: getBrregUnderenheter
      parameters:
        - in: path
          name: orgnr
          required: true
          schema:
            type: string
          description: 9-sifret organisasjonsnummer
      responses:
        200:
          description: Underenheter til brreg hovedenhet som matcher organisasjonsnummeret
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/BrregVirksomhet"

  /api/v1/intern/notifications:
    get:
      tags:
        - Notifications
      operationId: getNotifications
      parameters:
        - in: query
          name: status
          schema:
            $ref: "#/components/schemas/NotificationStatus"
          description: Status for notifikasjon
      responses:
        200:
          description: Notifications for the user that is currently signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaginertUserNotifications"

  /api/v1/intern/notifications/summary:
    get:
      tags:
        - Notifications
      operationId: getNotificationSummary
      responses:
        200:
          description: Notifications for the user that is currently signed in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserNotificationSummary"

  /api/v1/intern/notifications/status:
    post:
      tags:
        - Notifications
      operationId: setNotificationStatus
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SetNotificationStatusRequest"
        required: true
      responses:
        200:
          description: OK
          content:
            text/plain:
              schema:
                type: string

  /api/v1/intern/features:
    parameters:
      - in: query
        name: feature
        description: Navn på feature toggle
        required: true
        schema:
          $ref: "#/components/schemas/Toggles"

      - in: query
        name: tiltakskoder
        schema:
          type: array
          items:
            $ref: "#/components/schemas/Tiltakskode"
    get:
      tags:
        - FeatureToggle
      operationId: getFeatureToggle
      responses:
        200:
          description: Hent verdi for feature toggle
          content:
            application/json:
              schema:
                type: boolean

  /api/v1/intern/veileder/oppskrifter/{tiltakstypeId}:
    parameters:
      - in: path
        name: tiltakstypeId
        description: Id på tiltakstypen du skal hente oppskrifter for
        schema:
          type: string
        required: true
      - in: query
        name: perspective
        description: Hvilket Sanity-perspective du vil bruke
        schema:
          type: string
          enum:
            - previewDrafts
            - published
            - raw
    get:
      tags:
        - Oppskrifter
      operationId: getOppskrifter
      responses:
        200:
          description: Liste med oppskrifter tilknyttet tiltakstypen
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/Oppskrift"
                required:
                  - data

  /api/v1/intern/joyride/lagre:
    post:
      tags:
        - Joyride
      operationId: lagreJoyrideHarKjort
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VeilederJoyrideRequest"
      responses:
        204:
          description: "Lagret ok"
          content:
            text/plain:
              schema:
                type: string

  /api/v1/intern/joyride/{joyrideType}/har-fullfort:
    parameters:
      - in: path
        name: joyrideType
        description: Type for Joyride
        schema:
          $ref: "#/components/schemas/JoyrideType"
        required: true
    get:
      tags:
        - Joyride
      operationId: veilederHarFullfortJoyride
      responses:
        200:
          description: "True hvis veileder har fullført joyride, false hvis ikke"
          content:
            text/plain:
              schema:
                type: boolean

  /api/v1/intern/personopplysninger:
    get:
      tags:
        - Personopplysninger
      operationId: getPersonopplysninger
      responses:
        200:
          description: "Personopplysninger"
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PersonopplysningData"

  /api/v1/intern/opsjoner:
    post:
      tags:
        - Opsjoner
      operationId: lagreOpsjon
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OpsjonLoggRequest"
      responses:
        204:
          description: "Lagret ok"
          content:
            text/plain:
              schema:
                type: string
    delete:
      tags:
        - Opsjoner
      operationId: slettOpsjon
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SlettOpsjonLoggRequest"
      responses:
        204:
          description: "Sletting ok"
          content:
            text/plain:
              schema:
                type: string

  /api/v1/intern/lagret-filter:
    post:
      tags:
        - LagretFilter
      operationId: upsertFilter
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LagretFilterRequest"
      responses:
        204:
          description: "Lagret ok"
          content:
            text/plain:
              schema:
                type: string

  /api/v1/intern/lagret-filter/mine/{dokumenttype}:
    get:
      tags:
        - LagretFilter
      operationId: getMineFilterForDokumenttype
      parameters:
        - in: path
          name: dokumenttype
          description: type dokument
          schema:
            $ref: "#/components/schemas/LagretDokumenttype"
          required: true
      responses:
        200:
          description: "Lagrede filter for bruker for gitt dokumenttype"
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LagretFilter"

  /api/v1/intern/lagret-filter/{id}:
    delete:
      tags:
        - LagretFilter
      operationId: slettLagretFilter
      parameters:
        - in: path
          name: id
          description: Id for filter
          schema:
            type: string
            format: uuid
          required: true
      responses:
        204:
          description: "Ok ved sletting"
          content:
            text/plain:
              schema:
                type: string

  /api/v1/intern/tilsagn:
    put:
      tags:
        - Tilsagn
      operationId: opprettTilsagn
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TilsagnRequest"
      responses:
        200:
          description: TiltagnDto
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TilsagnDto"
    get:
      tags:
        - Tilsagn
      operationId: getAll
      parameters:
        - name: gjennomforingId
          in: query
          schema:
            type: string
            format: uuid
        - in: query
          name: statuser
          schema:
            type: array
            items:
              $ref: "#/components/schemas/TilsagnStatus"
      responses:
        200:
          description: TiltagnDto
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TilsagnDto"

  /api/v1/intern/gjennomforinger/{gjennomforingId}/utbetalinger:
    parameters:
      - $ref: "#/components/parameters/gjennomforingId"
    get:
      tags:
        - Utbetaling
      operationId: utbetalingerByGjennomforing
      responses:
        200:
          description: Utbetaling
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/UtbetalingDto"

  /api/v1/intern/utbetaling/{id}:
    parameters:
      - $ref: "#/components/parameters/ID"
    get:
      tags:
        - Utbetaling
      operationId: getUtbetaling
      responses:
        200:
          description: Utbetaling
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UtbetalingDetaljerDto"

  /api/v1/intern/utbetaling/{id}/historikk:
    get:
      tags:
        - Utbetaling
      operationId: getUtbetalingEndringshistorikk
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: ID for utbetaling
      responses:
        200:
          description: Endringshistorikken for utbetaling
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Endringshistorikk"

  /api/v1/intern/utbetaling/{id}/tilsagn:
    parameters:
      - $ref: "#/components/parameters/ID"
    get:
      tags:
        - Utbetaling
      operationId: getTilsagnTilUtbetaling
      responses:
        200:
          description: Tilsagn til utbetaling
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TilsagnDto"

  /api/v1/intern/delutbetalinger:
    parameters:
      - $ref: "#/components/parameters/ID"
    put:
      tags:
        - Utbetaling
      operationId: opprettDelutbetalinger
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OpprettDelutbetalingerRequest"
      responses:
        200:
          description: OK
        400:
          description: Respons med valideringsfeil
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationError"

  /api/v1/intern/delutbetalinger/{id}/beslutt:
    parameters:
      - $ref: "#/components/parameters/ID"
    post:
      tags:
        - Utbetaling
      operationId: besluttDelutbetaling
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BesluttDelutbetalingRequest"
      responses:
        200:
          description: OK
        400:
          description: Respons med valideringsfeil
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationError"

  /api/v1/intern/utbetaling/{id}/opprett-utbetaling:
    parameters:
      - $ref: "#/components/parameters/ID"
    post:
      tags:
        - Utbetaling
      operationId: opprettManuellUtbetaling
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OpprettManuellUtbetalingRequest"
      responses:
        200:
          description: OK
        400:
          description: Respons med valideringsfeil
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationError"
        500:
          description: "Kunne ikke opprette manuell utbetaling"
          content:
            application/json:
              schema:
                type: string

  /api/v1/intern/tilsagn/beregn:
    post:
      tags:
        - Beregn Tilsagn
      operationId: beregnTilsagn
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TilsagnBeregningInput"
      responses:
        200:
          description: TilsagnBeregning
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TilsagnBeregningOutput"

  /api/v1/intern/tilsagn/defaults:
    post:
      tags:
        - Tilsagn
      operationId: getTilsagnDefaults
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TilsagnDefaultsRequest"
      responses:
        200:
          description: Standardverdier for neste tilsagn
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TilsagnDefaults"

  /api/v1/intern/tilsagn/{id}:
    parameters:
      - $ref: "#/components/parameters/ID"
    get:
      tags:
        - Tilsagn
      operationId: getTilsagn
      responses:
        200:
          description: TilsagnDto
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TilsagnDetaljerDto"
    delete:
      tags:
        - Tilsagn
      operationId: slettTilsagn
      responses:
        200:
          description: OK

  /api/v1/intern/tilsagn/{id}/historikk:
    get:
      tags:
        - Tilsagn
      operationId: getTilsagnEndringshistorikk
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: ID for tilsagn
      responses:
        200:
          description: Endringshistorikken for tilsagn
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Endringshistorikk"

  /api/v1/intern/tilsagn/{id}/til-annullering:
    parameters:
      - $ref: "#/components/parameters/ID"
    post:
      tags:
        - Tilsagn
      operationId: tilAnnullering
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TilsagnTilAnnulleringRequest"
      responses:
        200:
          description: OK
          content:
            text/plain:
              schema:
                type: string

  /api/v1/intern/tilsagn/{id}/gjor-opp:
    parameters:
      - $ref: "#/components/parameters/ID"
    post:
      tags:
        - Tilsagn
      operationId: gjorOpp
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TilsagnTilAnnulleringRequest"
      responses:
        200:
          description: OK
          content:
            text/plain:
              schema:
                type: string

  /api/v1/intern/tilsagn/{id}/beslutt:
    parameters:
      - $ref: "#/components/parameters/ID"
    post:
      tags:
        - Tilsagn
      operationId: besluttTilsagn
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BesluttTilsagnRequest"
      responses:
        200:
          description: OK
          content:
            text/plain:
              schema:
                type: string

  /api/v1/intern/prismodell/satser:
    get:
      tags:
        - Prismodell
      parameters:
        - in: query
          name: tiltakstype
          required: true
          schema:
            $ref: "#/components/schemas/Tiltakskode"
            type: string
            format: uuid
      operationId: getForhandsgodkjenteSatser
      responses:
        200:
          description: Avtalte satser for avtale
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AvtaltSats"

  /api/v1/intern/arrangorflate/arrangor/{orgnr}/utbetaling:
    get:
      tags:
        - Arrangorflate
      parameters:
        - $ref: "#/components/parameters/OrgnrParam"
      operationId: getAllUtbetaling
      responses:
        200:
          description: Hent utbetaling til arrangør
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ArrFlateUtbetalingKompakt"
        default:
          $ref: "#/components/responses/ProblemDetail"
    post:
      tags:
        - Arrangorflate
      parameters:
        - $ref: "#/components/parameters/OrgnrParam"
      operationId: opprettArrangorflateManuellUtbetaling
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ArrangorflateManuellUtbetalingRequest"
      responses:
        200:
          description: Lag manuell utbetaling
          content:
            text/plain:
              schema:
                type: string
                format: uuid
        400:
          $ref: "#/components/responses/ValidationError"
        default:
          $ref: "#/components/responses/ProblemDetail"

  /api/v1/intern/arrangorflate/arrangor/{orgnr}/tilsagn:
    get:
      tags:
        - Arrangorflate
      operationId: getAllArrangorflateTilsagn
      parameters:
        - $ref: "#/components/parameters/OrgnrParam"
      responses:
        200:
          description: Hent tilsagn til arrangør
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ArrangorflateTilsagn"
        default:
          $ref: "#/components/responses/ProblemDetail"

  /api/v1/intern/arrangorflate/utbetaling/{id}/tilsagn:
    get:
      tags:
        - Arrangorflate
      operationId: getArrangorflateTilsagnTilUtbetaling
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        200:
          description: Hent tilsagn til utbetaling
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ArrangorflateTilsagn"
        default:
          $ref: "#/components/responses/ProblemDetail"

  /api/v1/intern/arrangorflate/arrangor/{orgnr}/gjennomforing:
    get:
      tags:
        - Arrangorflate
      operationId: getArrangorflateGjennomforinger
      parameters:
        - $ref: "#/components/parameters/OrgnrParam"
      responses:
        200:
          description: Get gjennomforinger til arrangør
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ArrangorflateGjennomforing"
        default:
          $ref: "#/components/responses/ProblemDetail"

  /api/v1/intern/arrangorflate/arrangor/{orgnr}/kontonummer:
    get:
      tags:
        - Arrangorflate
      operationId: getKontonummer
      parameters:
        - $ref: "#/components/parameters/OrgnrParam"
      responses:
        200:
          description: Get kontonummer til arrangør
          content:
            text/plain:
              schema:
                type: string
        default:
          $ref: "#/components/responses/ProblemDetail"

  /api/v1/intern/arrangorflate/utbetaling/{id}/sync-kontonummer:
    get:
      tags:
        - Arrangorflate
      operationId: synkroniserKontonummerForUtbetaling
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        200:
          description: Synkroniserer kontonummer for utbetaling
          content:
            text/plain:
              schema:
                type: string
        default:
          $ref: "#/components/responses/ProblemDetail"

  /api/v1/intern/arrangorflate/utbetaling/{id}/relevante-forslag:
    get:
      tags:
        - Arrangorflate
      operationId: getRelevanteForslag
      parameters:
        - $ref: "#/components/parameters/ID"
      responses:
        200:
          description: Hent relevante deltakerforslag til deltakere på utbetaling
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/RelevanteForslag"
        default:
          $ref: "#/components/responses/ProblemDetail"

  /api/v1/intern/arrangorflate/tilsagn/{id}:
    get:
      tags:
        - Arrangorflate
      parameters:
        - $ref: "#/components/parameters/ID"
      operationId: getArrangorflateTilsagn
      responses:
        200:
          description: Hent tilsagn by id
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ArrangorflateTilsagn"
        default:
          $ref: "#/components/responses/ProblemDetail"

  /api/v1/intern/arrangorflate/utbetaling/{id}:
    get:
      parameters:
        - $ref: "#/components/parameters/ID"
      tags:
        - Arrangorflate
      operationId: getArrFlateUtbetaling
      responses:
        200:
          description: Hent utbetaling til arrangør
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ArrFlateUtbetaling"
        default:
          $ref: "#/components/responses/ProblemDetail"

  /api/v1/intern/arrangorflate/utbetaling/{id}/godkjenn:
    post:
      parameters:
        - $ref: "#/components/parameters/ID"
      tags:
        - Arrangorflate
      operationId: godkjennUtbetaling
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GodkjennUtbetaling"
        required: true
      responses:
        200:
          description: Godkjenn et utbetaling
        400:
          description: Respons med valideringsfeil
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationError"
        default:
          $ref: "#/components/responses/ProblemDetail"

  /api/v1/intern/arrangorflate/utbetaling/{id}/kvittering:
    parameters:
      - $ref: "#/components/parameters/ID"
    get:
      tags:
        - Arrangorflate
      operationId: getUtbetalingKvittering
      responses:
        200:
          description: Generer PDF kvittering for utbetaling
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        default:
          $ref: "#/components/responses/ProblemDetail"

  /api/v1/intern/arrangorflate/tilgang-arrangor:
    get:
      tags:
        - Arrangorflate
      operationId: getArrangorerInnloggetBrukerHarTilgangTil
      responses:
        200:
          description: Henter liste over arrangører innlogget bruker har tilgang til
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Arrangor"
        default:
          $ref: "#/components/responses/ProblemDetail"

  /api/v1/intern/utdanninger:
    get:
      tags:
        - Utdanninger
      operationId: getUtdanningsprogrammer
      responses:
        200:
          description: Alle utdanningsprogram med utdanninger
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/UtdanningsprogramMedUtdanninger"

  /api/v1/intern/oppgaver:
    post:
      tags:
        - Oppgaver
      operationId: getOppgaver
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GetOppgaverRequest"
        required: true
      responses:
        200:
          description: Notifications for the user that is currently signed in.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Oppgave"

components:
  responses:
    ProblemDetail:
      description: Common error type
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ProblemDetail"
    ValidationError:
      description: Validation error
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ValidationError"
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  parameters:
    ID:
      name: id
      in: path
      description: ID
      required: true
      schema:
        type: string
        format: uuid

    gjennomforingId:
      name: gjennomforingId
      in: path
      description: Gjennomforing Id
      required: true
      schema:
        type: string
        format: uuid

    TiltakskodeParam:
      name: tiltakskode
      in: path
      description: TiltakskodeArena
      required: true
      schema:
        $ref: "#/components/schemas/TiltakskodeArena"

    EnheterParam:
      in: query
      name: enheter
      required: true
      schema:
        type: array
        items:
          type: string

    InnsatsgruppeParam:
      in: query
      name: innsatsgruppe
      schema:
        $ref: "#/components/schemas/Innsatsgruppe"

    ErSykmeldtMedArbeidsgiverParam:
      in: query
      name: erSykmeldtMedArbeidsgiver
      schema:
        type: boolean

    TiltakstyperParam:
      in: query
      name: tiltakstyper
      schema:
        type: array
        items:
          type: string
          format: uuid

    SearchParam:
      in: query
      name: search
      schema:
        type: string

    ApentForPameldingParam:
      in: query
      name: apentForPamelding
      required: true
      schema:
        $ref: "#/components/schemas/ApentForPamelding"

    OrgnrParam:
      in: path
      name: orgnr
      description: organisasjonsnummer
      required: true
      schema:
        type: string

  schemas:
    ProblemDetail:
      type: object
      properties:
        type:
          type: string
        title:
          type: string
        status:
          type: number
        detail:
          type: string
        instance:
          type: string
      required: [type, title, status, detail]
      additionalProperties: true

    ValidationError:
      allOf:
        - $ref: "#/components/schemas/ProblemDetail"
        - type: object
          properties:
            errors:
              type: array
              items:
                $ref: "#/components/schemas/FieldError"
          required: [errors]

    FieldError:
      type: object
      properties:
        detail:
          type: string
        pointer:
          type: string
      required: [detail, pointer]

    GetBrukerRequest:
      type: object
      properties:
        norskIdent:
          type: string
      required:
        - norskIdent

    GetDeltakelserForBrukerRequest:
      type: object
      properties:
        norskIdent:
          type: string
        type:
          type: string
          enum:
            - AKTIVE
            - HISTORISKE
      required:
        - norskIdent
        - type

    GetAktivDeltakelseForBrukerRequest:
      type: object
      properties:
        norskIdent:
          type: string
        tiltakId:
          type: string
          format: uuid
      required:
        - norskIdent
        - tiltakId

    GetDelMedBrukerRequest:
      type: object
      properties:
        norskIdent:
          type: string
        tiltakId:
          type: string
          format: uuid
      required:
        - norskIdent
        - tiltakId

    GetAlleDeltMedBrukerRequest:
      type: object
      properties:
        norskIdent:
          type: string
      required:
        - norskIdent

    ApentForPamelding:
      type: string
      enum:
        - APENT
        - STENGT
        - APENT_ELLER_STENGT

    ManuellStatus:
      type: object
      properties:
        erUnderManuellOppfolging:
          type: boolean
        krrStatus:
          type: object
          properties:
            kanVarsles:
              type: boolean
            erReservert:
              type: boolean
          required:
            - kanVarsles
            - erReservert
      required:
        - erUnderManuellOppfolging
        - krrStatus

    Pagination:
      type: object
      properties:
        totalCount:
          type: integer
        totalPages:
          type: integer
        pageSize:
          type: integer
      required:
        - totalCount
        - totalPages
        - pageSize

    PaginertTiltakstype:
      type: object
      required:
        - pagination
        - data
      properties:
        pagination:
          $ref: "#/components/schemas/Pagination"
        data:
          type: array
          items:
            $ref: "#/components/schemas/TiltakstypeDto"

    PaginertGjennomforing:
      type: object
      required:
        - pagination
        - data
      properties:
        pagination:
          $ref: "#/components/schemas/Pagination"
        data:
          type: array
          items:
            $ref: "#/components/schemas/GjennomforingDto"

    PaginertAvtale:
      type: object
      required:
        - pagination
        - data
      properties:
        pagination:
          $ref: "#/components/schemas/Pagination"
        data:
          type: array
          items:
            $ref: "#/components/schemas/AvtaleDto"

    PaginertArrangor:
      type: object
      required:
        - pagination
        - data
      properties:
        pagination:
          $ref: "#/components/schemas/Pagination"
        data:
          type: array
          items:
            $ref: "#/components/schemas/Arrangor"

    TiltakskodeArena:
      type: string
      enum:
        - IPSUNG
        - INKLUTILS
        - HOYEREUTD
        - ENKELAMO
        - GRUFAGYRKE
        - ARBTREN
        - ARBFORB
        - MIDLONTIL
        - TILSJOBB
        - ENKFAGYRKE
        - INDJOBSTOT
        - AVKLARAG
        - DIGIOPPARB
        - MENTOR
        - INDOPPFAG
        - GRUPPEAMO
        - VASV
        - ARBRRHDAG
        - JOBBK
        - VATIAROR
        - VARLONTIL
        - UTVAOONAV

    TiltakstypeDto:
      type: object
      properties:
        id:
          type: string
          format: uuid
        navn:
          type: string
        innsatsgrupper:
          type: array
          items:
            $ref: "#/components/schemas/Innsatsgruppe"
        tiltakskode:
          $ref: "#/components/schemas/Tiltakskode"
        arenaKode:
          $ref: "#/components/schemas/TiltakskodeArena"
        startDato:
          type: string
          format: date
        sluttDato:
          type: string
          format: date
        status:
          $ref: "#/components/schemas/TiltakstypeStatus"
        sanityId:
          type: string
      required:
        - id
        - navn
        - arenaKode
        - startDato
        - status

    AvtaleDto:
      type: object
      properties:
        id:
          type: string
          format: uuid
        tiltakstype:
          $ref: "#/components/schemas/EmbeddedTiltakstype"
        navn:
          type: string
        avtalenummer:
          type: string
        sakarkivNummer:
          type: [string, "null"]
        arrangor:
          $ref: "#/components/schemas/ArrangorHovedenhet"
        startDato:
          type: string
          format: date
        sluttDato:
          type: [string, "null"]
          format: date
        avtaletype:
          $ref: "#/components/schemas/Avtaletype"
        status:
          $ref: "#/components/schemas/AvtalestatusResponse"
        arenaAnsvarligEnhet:
          $ref: "#/components/schemas/ArenaNavEnhet"
        prisbetingelser:
          type: [string, "null"]
        administratorer:
          type: array
          items:
            $ref: "#/components/schemas/AvtaleAdministrator"
        antallPlasser:
          type: [number, "null"]
        kontorstruktur:
          $ref: "#/components/schemas/NavKontorStruktur"
        opphav:
          $ref: "#/components/schemas/Opphav"
        beskrivelse:
          type: string
        faneinnhold:
          $ref: "#/components/schemas/Faneinnhold"
        personopplysninger:
          type: array
          items:
            $ref: "#/components/schemas/Personopplysning"
        personvernBekreftet:
          type: boolean
        amoKategorisering:
          anyOf:
            - type: ["null"]
            - $ref: "#/components/schemas/AmoKategorisering"
        opsjonsmodellData:
          anyOf:
            - type: ["null"]
            - $ref: "#/components/schemas/OpsjonsmodellData"
        opsjonerRegistrert:
          type: array
          items:
            $ref: "#/components/schemas/OpsjonLoggRegistrert"
        utdanningslop:
          anyOf:
            - type: ["null"]
            - $ref: "#/components/schemas/Utdanningslop"
        prismodell:
          anyOf:
            - type: ["null"]
            - $ref: "#/components/schemas/Prismodell"
      required:
        - id
        - tiltakstype
        - navn
        - avtalenummer
        - startDato
        - avtaletype
        - status
        - prisbetingelser
        - kontorstruktur
        - opphav
        - personopplysninger
        - personvernBekreftet
        - amoKategorisering
        - opsjonsmodellData
        - opsjonerRegistrert
        - utdanningslop
        - prismodell

    AvtaleRequest:
      type: object
      properties:
        id:
          type: string
          format: UUID
        navn:
          type: string
        tiltakstypeId:
          type: string
          format: UUID
        avtalenummer:
          type: [string, "null"]
        sakarkivNummer:
          type: [string, "null"]
        arrangor:
          anyOf:
            - type: "null"
            - type: object
              properties:
                hovedenhet:
                  type: string
                underenheter:
                  type: array
                  items:
                    type: string
                kontaktpersoner:
                  type: array
                  items:
                    type: string
                    format: UUID
              required:
                - hovedenhet
                - underenheter
                - kontaktpersoner
        startDato:
          type: string
          format: date
        sluttDato:
          type: [string, "null"]
          format: date
        administratorer:
          type: array
          items:
            type: string
        avtaletype:
          $ref: "#/components/schemas/Avtaletype"
        prisbetingelser:
          type: [string, "null"]
        navEnheter:
          type: array
          items:
            type: string
        beskrivelse:
          type: [string, "null"]
        faneinnhold:
          anyOf:
            - type: "null"
            - $ref: "#/components/schemas/FaneinnholdRequest"
        personopplysninger:
          type: array
          items:
            $ref: "#/components/schemas/Personopplysning"
        personvernBekreftet:
          type: boolean
        amoKategorisering:
          anyOf:
            - type: "null"
            - $ref: "#/components/schemas/AmoKategorisering"
        opsjonsmodellData:
          anyOf:
            - type: "null"
            - $ref: "#/components/schemas/OpsjonsmodellData"
        utdanningslop:
          anyOf:
            - type: "null"
            - $ref: "#/components/schemas/UtdanningslopDbo"
        prismodell:
          anyOf:
            - type: "null"
            - $ref: "#/components/schemas/Prismodell"
      required:
        - id
        - navn
        - tiltakstypeId
        - arrangorOrganisasjonsnummer
        - arrangorUnderenheter
        - arrangorKontaktpersonId
        - avtalenummer
        - startDato
        - sluttDato
        - administrator
        - avtaletype
        - prisbetingelser
        - navEnheter
        - beskrivelse
        - faneinnhold
        - personopplysninger
        - personvernBekreftet
        - amoKategorisering
        - opsjonsmodellData
        - utdanningslop
        - prismodell

    Prismodell:
      type: string
      enum:
        - FORHANDSGODKJENT
        - FRI

    EmbeddedTiltakstype:
      type: object
      properties:
        id:
          type: string
          format: uuid
        navn:
          type: string
        tiltakskode:
          $ref: "#/components/schemas/Tiltakskode"
      required:
        - id
        - navn
        - arenaKode
        - tiltakskode

    Avtaletype:
      type: string
      enum:
        - Avtale
        - Rammeavtale
        - Forhaandsgodkjent
        - OffentligOffentlig

    AvtalestatusResponse:
      oneOf:
        - $ref: "#/components/schemas/AvtaleIkkeAvbruttStatus"
        - $ref: "#/components/schemas/AvtaleAvbruttStatus"

    AvtaleIkkeAvbruttStatus:
      type: object
      properties:
        name:
          type: string
          enum:
            - AKTIV
            - AVSLUTTET
            - UTKAST
      required:
        - name

    AvtaleAvbruttStatus:
      type: object
      properties:
        name:
          type: string
          enum:
            - AVBRUTT
        tidspunkt:
          type: string
          format: date-time
        aarsak:
          oneOf:
            - type: string
            - $ref: "#/components/schemas/AvbrytAvtaleAarsak"
      required:
        - name
        - tidspunkt
        - aarsak

    Avtalestatus:
      type: string
      enum:
        - AKTIV
        - AVSLUTTET
        - AVBRUTT
        - UTKAST

    NotificationStatus:
      type: string
      enum:
        - DONE
        - NOT_DONE

    OppgaveType:
      type: string
      enum:
        - TILSAGN_TIL_GODKJENNING
        - TILSAGN_TIL_ANNULLERING
        - TILSAGN_TIL_OPPGJOR
        - TILSAGN_RETURNERT
        - UTBETALING_TIL_BEHANDLING
        - UTBETALING_TIL_GODKJENNING
        - UTBETALING_RETURNERT

    SorteringAvtaler:
      type: string
      enum:
        - navn-ascending
        - navn-descending
        - startdato-ascending
        - startdato-descending
        - sluttdato-ascending
        - sluttdato-descending
        - arrangor-ascending
        - arrangor-descending

    SorteringTiltakstyper:
      type: string
      enum:
        - navn-ascending
        - navn-descending
        - startdato-ascending
        - startdato-descending
        - sluttdato-ascending
        - sluttdato-descending

    SorteringGjennomforinger:
      type: string
      enum:
        - navn-ascending
        - navn-descending
        - tiltaksnummer-ascending
        - tiltaksnummer-descending
        - tiltakstype-ascending
        - tiltakstype-descending
        - startdato-ascending
        - startdato-descending
        - sluttdato-ascending
        - sluttdato-descending
        - arrangor-ascending
        - arrangor-descending
        - publisert_ascending
        - publisert_descending

    SorteringArrangorer:
      type: string
      enum:
        - navn-ascending
        - navn-descending

    GjennomforingDto:
      type: object
      properties:
        id:
          type: string
          format: uuid
        tiltakstype:
          $ref: "#/components/schemas/EmbeddedTiltakstype"
        navn:
          type: string
        tiltaksnummer:
          type: [string, "null"]
        arrangor:
          $ref: "#/components/schemas/ArrangorUnderenhet"
        startDato:
          type: string
          format: date
        sluttDato:
          type: string
          format: date
        arenaAnsvarligEnhet:
          $ref: "#/components/schemas/ArenaNavEnhet"
        status:
          $ref: "#/components/schemas/GjennomforingStatusDto"
        apentForPamelding:
          type: boolean
        antallPlasser:
          type: integer
        avtaleId:
          type: string
          format: uuid
        administratorer:
          type: array
          items:
            $ref: "#/components/schemas/GjennomforingAdministrator"
        kontorstruktur:
          $ref: "#/components/schemas/NavKontorStruktur"
        sanityId:
          type: string
          format: uuid
        oppstart:
          $ref: "#/components/schemas/GjennomforingOppstartstype"
        opphav:
          $ref: "#/components/schemas/Opphav"
        kontaktpersoner:
          type: array
          items:
            $ref: "#/components/schemas/GjennomforingKontaktperson"
        stedForGjennomforing:
          type: string
        faneinnhold:
          $ref: "#/components/schemas/Faneinnhold"
        beskrivelse:
          type: string
        publisert:
          type: boolean
        deltidsprosent:
          type: number
        estimertVentetid:
          anyOf:
            - type: ["null"]
            - $ref: "#/components/schemas/EstimertVentetid"
        tilgjengeligForArrangorFraOgMedDato:
          type: [string, "null"]
          format: date
        amoKategorisering:
          anyOf:
            - type: ["null"]
            - $ref: "#/components/schemas/AmoKategorisering"
        utdanningslop:
          anyOf:
            - type: ["null"]
            - $ref: "#/components/schemas/Utdanningslop"
        stengt:
          type: array
          items:
            $ref: "#/components/schemas/StengtPeriode"
      required:
        - id
        - tiltakstype
        - navn
        - tiltaksnummer
        - arrangor
        - startDato
        - sluttDato
        - status
        - apentForPamelding
        - administratorer
        - kontorstruktur
        - oppstart
        - opphav
        - kontaktpersoner
        - publisert
        - deltidsprosent
        - tilgjengeligForArrangorFraOgMedDato
        - amoKategorisering
        - stengt

    TiltaksnummerResponse:
      type: object
      properties:
        tiltaksnummer:
          type: string
      required:
        - tiltaksnummer

    GjennomforingRequest:
      type: object
      properties:
        id:
          type: string
          format: UUID
        tiltakstypeId:
          type: string
          format: UUID
        avtaleId:
          type: string
          format: UUID
        navn:
          type: string
        startDato:
          type: string
          format: date
        sluttDato:
          type: [string, "null"]
          format: date
        antallPlasser:
          type: number
        arrangorId:
          type: string
          format: uuid
        arrangorKontaktpersoner:
          type: array
          items:
            type: string
            format: UUID
        administratorer:
          type: array
          items:
            type: string
        navEnheter:
          type: array
          items:
            type: string
        oppstart:
          $ref: "#/components/schemas/GjennomforingOppstartstype"
        kontaktpersoner:
          type: array
          items:
            $ref: "#/components/schemas/GjennomforingKontaktpersonRequest"
        stedForGjennomforing:
          type: [string, "null"]
        faneinnhold:
          anyOf:
            - type: ["null"]
            - $ref: "#/components/schemas/FaneinnholdRequest"
        beskrivelse:
          type: [string, "null"]
        deltidsprosent:
          type: number
        estimertVentetid:
          anyOf:
            - type: "null"
            - type: object
              properties:
                verdi:
                  type: number
                enhet:
                  type: string
        tilgjengeligForArrangorFraOgMedDato:
          type: [string, "null"]
          format: date
        amoKategorisering:
          anyOf:
            - type: ["null"]
            - $ref: "#/components/schemas/AmoKategorisering"
        utdanningslop:
          anyOf:
            - type: ["null"]
            - $ref: "#/components/schemas/UtdanningslopDbo"
      required:
        - id
        - tiltakstypeId
        - avtaleId
        - navn
        - startDato
        - sluttDato
        - antallPlasser
        - arrangorId
        - arrangorKontaktpersoner
        - administrator
        - navEnheter
        - oppstart
        - kontaktpersoner
        - stedForGjennomforing
        - faneinnhold
        - beskrivelse
        - deltidsprosent
        - estimertVentetid
        - tilgjengeligForArrangorFraOgMedDato
        - amoKategorisering
        - utdanningslop

    GjennomforingStatusDto:
      type: object
      properties:
        status:
          $ref: "#/components/schemas/GjennomforingStatus"
        avbrutt:
          type: object
          properties:
            tidspunkt:
              type: string
              format: date-time
            aarsak:
              oneOf:
                - type: string
                - $ref: "#/components/schemas/AvbrytGjennomforingAarsak"
            beskrivelse:
              type: string
          required:
            - tidspunkt
            - aarsak
            - beskrivelse
      required:
        - status

    GjennomforingStatus:
      type: string
      enum:
        - GJENNOMFORES
        - AVBRUTT
        - AVLYST
        - AVSLUTTET

    GjennomforingOppstartstype:
      type: string
      enum:
        - LOPENDE
        - FELLES

    Opphav:
      type: string
      enum:
        - ARENA
        - MR_ADMIN_FLATE

    ArrangorTil:
      type: string
      enum:
        - AVTALE
        - TILTAKSGJENNOMFORING

    Bruker:
      type: object
      properties:
        fnr:
          type: string
        erSykmeldtMedArbeidsgiver:
          type: boolean
        innsatsgruppe:
          $ref: "#/components/schemas/Innsatsgruppe"
        enheter:
          type: array
          items:
            $ref: "#/components/schemas/NavEnhet"
        fornavn:
          type: string
        manuellStatus:
          $ref: "#/components/schemas/ManuellStatus"
        erUnderOppfolging:
          type: boolean
        varsler:
          type: array
          items:
            $ref: "#/components/schemas/BrukerVarsel"
      required:
        - erSykmeldtMedArbeidsgiver
        - manuellStatus
        - fnr
        - enheter
        - varsler
        - erUnderOppfolging

    BrukerVarsel:
      type: string
      enum:
        - LOKAL_OPPFOLGINGSENHET
        - BRUKER_IKKE_UNDER_OPPFOLGING
        - BRUKER_UNDER_OPPFOLGING_MEN_MANGLER_14A_VEDTAK

    NavVeileder:
      type: object
      properties:
        navIdent:
          type: string
        fornavn:
          type: string
        etternavn:
          type: string
        hovedenhet:
          $ref: "#/components/schemas/NavVeilederHovedenhet"
      required:
        - navIdent
        - fornavn
        - etternavn
        - hovedenhet

    NavVeilederHovedenhet:
      type: object
      properties:
        enhetsnummer:
          type: string
        navn:
          type: string
      required:
        - enhetsnummer
        - navn

    NavAnsatt:
      type: object
      properties:
        azureId:
          type: string
          format: uuid
        navIdent:
          type: string
        fornavn:
          type: string
        etternavn:
          type: string
        hovedenhet:
          $ref: "#/components/schemas/NavAnsattHovedenhet"
        mobilnummer:
          type: [string, "null"]
        epost:
          type: string
        roller:
          type: array
          items:
            $ref: "#/components/schemas/Rolle"
        skalSlettesDato:
          type: [string, "null"]
          format: date
      required:
        - azureId
        - navIdent
        - fornavn
        - etternavn
        - hovedenhet
        - mobilnummer
        - epost
        - roller
        - skalSlettesDato

    Rolle:
      type: string
      enum:
        - TEAM_MULIGHETSROMMET
        - KONTAKTPERSON
        - TILTAKADMINISTRASJON_GENERELL
        - TILTAKADMINISTRASJON_ENDRINGSMELDING
        - TILTAKSGJENNOMFORINGER_SKRIV
        - AVTALER_SKRIV
        - SAKSBEHANDLER_OKONOMI
        - BESLUTTER_TILSAGN
        - ATTESTANT_UTBETALING

    NavAnsattHovedenhet:
      type: object
      properties:
        enhetsnummer:
          type: string
        navn:
          type: string
      required:
        - enhetsnummer
        - navn

    Innsatsgruppe:
      type: string
      enum:
        - GODE_MULIGHETER
        - TRENGER_VEILEDNING
        - TRENGER_VEILEDNING_NEDSATT_ARBEIDSEVNE
        - JOBBE_DELVIS
        - LITEN_MULIGHET_TIL_A_JOBBE

    TiltakstypeStatus:
      type: string
      enum:
        - AKTIV
        - AVSLUTTET

    DelTiltakMedBrukerRequest:
      type: object
      properties:
        fnr:
          type: string
        overskrift:
          type: string
        tekst:
          type: string
        venterPaaSvarFraBruker:
          type: boolean
        gjennomforingId:
          type: [string, "null"]
          format: uuid
        sanityId:
          type: [string, "null"]
          format: uuid
      required:
        - fnr
        - overskrift
        - tekst
        - venterPaaSvarFraBruker
        - gjennomforingId
        - sanityId

    DelTiltakMedBrukerResponse:
      type: object
      properties:
        dialogId:
          type: string
      required:
        - dialogId

    GetDeltakelserForBrukerResponse:
      type: object
      properties:
        meldinger:
          type: array
          items:
            $ref: "#/components/schemas/DeltakelserMelding"
        deltakelser:
          type: array
          items:
            $ref: "#/components/schemas/Deltakelse"
      required:
        - meldinger
        - deltakelser

    DeltakelserMelding:
      type: string
      enum:
        - MANGLER_SISTE_DELTAKELSER_FRA_TEAM_KOMET
        - MANGLER_DELTAKELSER_FRA_TEAM_TILTAK

    Deltakelse:
      oneOf:
        - $ref: "#/components/schemas/DeltakelseArena"
        - $ref: "#/components/schemas/DeltakelseGruppetiltak"
        - $ref: "#/components/schemas/DeltakelseArbeidsgiverAvtale"

    DeltakelsePeriode:
      type: object
      properties:
        startDato:
          type: [string, "null"]
          format: date
        sluttDato:
          type: [string, "null"]
          format: date

    DeltakelseEierskap:
      type: string
      enum:
        - ARENA
        - TEAM_KOMET
        - TEAM_TILTAK

    DeltakelseArena:
      type: object
      properties:
        id:
          type: string
          format: uuid
        eierskap:
          const: ARENA
        tittel:
          type: string
        tiltakstypeNavn:
          type: string
        innsoktDato:
          type: string
        sistEndretDato:
          type: string
        periode:
          $ref: "#/components/schemas/DeltakelsePeriode"
        status:
          type: object
          properties:
            type:
              $ref: "#/components/schemas/ArenaDeltakerStatus"
            visningstekst:
              type: string
          required:
            - type
            - visningstekst
      required:
        - id
        - eierskap
        - tittel
        - tiltakstypeNavn
        - periode
        - status

    ArenaDeltakerStatus:
      type: string
      enum:
        - AVSLAG
        - IKKE_AKTUELL
        - TAKKET_NEI_TIL_TILBUD
        - TILBUD
        - TAKKET_JA_TIL_TILBUD
        - INFORMASJONSMOTE
        - AKTUELL
        - VENTELISTE
        - GJENNOMFORES
        - DELTAKELSE_AVBRUTT
        - GJENNOMFORING_AVBRUTT
        - GJENNOMFORING_AVLYST
        - FULLFORT
        - IKKE_MOTT
        - FEILREGISTRERT

    DeltakelseGruppetiltak:
      type: object
      properties:
        id:
          type: string
          format: uuid
        eierskap:
          type: string
          enum:
            - ARENA
            - TEAM_KOMET
        tittel:
          type: string
        tiltakstypeNavn:
          type: string
        innsoktDato:
          type: string
        sistEndretDato:
          type: string
        periode:
          $ref: "#/components/schemas/DeltakelsePeriode"
        status:
          type: object
          properties:
            type:
              $ref: "#/components/schemas/GruppetiltakDeltakerStatus"
            visningstekst:
              type: string
            aarsak:
              type: string
          required:
            - type
            - visningstekst
        gjennomforingId:
          type: string
          format: uuid
      required:
        - id
        - eierskap
        - tittel
        - tiltakstypeNavn
        - periode
        - status
        - gjennomforingId

    GruppetiltakDeltakerStatus:
      type: string
      enum:
        - VENTER_PA_OPPSTART
        - DELTAR
        - HAR_SLUTTET
        - IKKE_AKTUELL
        - FEILREGISTRERT
        - PABEGYNT_REGISTRERING
        - SOKT_INN
        - VURDERES
        - VENTELISTE
        - AVBRUTT
        - FULLFORT
        - UTKAST_TIL_PAMELDING
        - AVBRUTT_UTKAST
        - KLADD

    DeltakelseArbeidsgiverAvtale:
      type: object
      properties:
        id:
          type: string
          format: uuid
        eierskap:
          const: TEAM_TILTAK
        tittel:
          type: string
        tiltakstypeNavn:
          type: string
        innsoktDato:
          type: string
        sistEndretDato:
          type: string
        periode:
          $ref: "#/components/schemas/DeltakelsePeriode"
        status:
          type: object
          properties:
            type:
              $ref: "#/components/schemas/ArbeidsgiverAvtaleStatus"
            visningstekst:
              type: string
          required:
            - type
            - visningstekst
      required:
        - id
        - eierskap
        - tittel
        - tiltakstypeNavn
        - periode
        - status

    ArbeidsgiverAvtaleStatus:
      type: string
      enum:
        - ANNULLERT
        - AVBRUTT
        - PAABEGYNT
        - MANGLER_GODKJENNING
        - KLAR_FOR_OPPSTART
        - GJENNOMFORES
        - AVSLUTTET

    DelMedBruker:
      type: object
      properties:
        id:
          type: string
        norskIdent:
          type: string
        navident:
          type: string
        sanityId:
          type: string
          format: uuid
        gjennomforingId:
          type: string
          format: uuid
        dialogId:
          type: string
        createdAt:
          type: string
        updatedAt:
          type: string
        createdBy:
          type: string
        updatedBy:
          type: string
      required:
        - norskIdent
        - navident
        - dialogId

    ArenaNavEnhet:
      type: object
      properties:
        enhetsnummer:
          type: string
        navn:
          type: string
      required:
        - enhetsnummer

    NavEnhet:
      type: object
      properties:
        enhetsnummer:
          type: string
        overordnetEnhet:
          type: [string, "null"]
        navn:
          type: string
        status:
          $ref: "#/components/schemas/NavEnhetStatus"
        type:
          $ref: "#/components/schemas/NavEnhetType"
      required:
        - navn
        - overordnetEnhet
        - enhetsnummer
        - status
        - type

    NavRegion:
      type: object
      properties:
        enhetsnummer:
          type: string
        navn:
          type: string
        status:
          $ref: "#/components/schemas/NavEnhetStatus"
        type:
          $ref: "#/components/schemas/NavEnhetType"
        enheter:
          type: array
          items:
            $ref: "#/components/schemas/NavEnhet"
      required:
        - navn
        - enheter
        - enhetsnummer
        - status
        - type

    ArrangorHovedenhet:
      type: object
      properties:
        id:
          type: string
          format: uuid
        organisasjonsnummer:
          type: string
        navn:
          type: string
        slettet:
          type: boolean
        underenheter:
          type: array
          items:
            $ref: "#/components/schemas/ArrangorUnderenhet"
        kontaktpersoner:
          type: array
          items:
            $ref: "#/components/schemas/ArrangorKontaktperson"
      required:
        - id
        - organisasjonsnummer
        - navn
        - slettet
        - underenheter
        - kontaktpersoner

    ArrangorUnderenhet:
      type: object
      properties:
        id:
          type: string
          format: uuid
        organisasjonsnummer:
          type: string
        navn:
          type: string
        kontaktpersoner:
          type: array
          items:
            $ref: "#/components/schemas/ArrangorKontaktperson"
        slettet:
          type: boolean
      required:
        - id
        - organisasjonsnummer
        - navn
        - kontaktpersoner
        - slettet

    ArrangorKontaktpersonRequest:
      type: object
      properties:
        id:
          type: string
          format: uuid
        navn:
          type: string
        telefon:
          type: [string, "null"]
        beskrivelse:
          type: [string, "null"]
        epost:
          type: string
        ansvarligFor:
          type: array
          items:
            $ref: "#/components/schemas/ArrangorKontaktpersonAnsvar"
      required:
        - id
        - navn
        - epost
        - telefon
        - beskrivelse
        - ansvarligFor

    ArrangorKontaktperson:
      type: object
      properties:
        id:
          type: string
          format: uuid
        arrangorId:
          type: string
          format: uuid
        navn:
          type: string
        beskrivelse:
          type: [string, "null"]
        telefon:
          type: [string, "null"]
        epost:
          type: string
        ansvarligFor:
          type: array
          items:
            $ref: "#/components/schemas/ArrangorKontaktpersonAnsvar"
      required:
        - id
        - arrangorId
        - navn
        - beskrivelse
        - telefon
        - epost
        - ansvarligFor

    ArrangorKontaktpersonAnsvar:
      type: string
      enum:
        - AVTALE
        - GJENNOMFORING
        - OKONOMI

    AvtaleAdministrator:
      type: object
      properties:
        navIdent:
          type: string
        navn:
          type: string
      required:
        - navIdent
        - navn

    GjennomforingAdministrator:
      type: object
      properties:
        navIdent:
          type: string
        navn:
          type: string
      required:
        - navIdent
        - navn

    VeilederflateInnsatsgruppe:
      type: object
      properties:
        tittel:
          type: string
        nokkel:
          $ref: "#/components/schemas/Innsatsgruppe"
        order:
          type: integer
      required:
        - tittel
        - nokkel
        - order

    VeilederflateTiltakstype:
      type: object
      properties:
        sanityId:
          type: string
        navn:
          type: string
        beskrivelse:
          type: string
        innsatsgrupper:
          type: array
          uniqueItems: true
          items:
            $ref: "#/components/schemas/Innsatsgruppe"
        regelverkLenker:
          type: array
          items:
            $ref: "#/components/schemas/RegelverkLenke"
        faneinnhold:
          $ref: "#/components/schemas/Faneinnhold"
        delingMedBruker:
          type: string
        arenakode:
          $ref: "#/components/schemas/TiltakskodeArena"
        tiltakskode:
          $ref: "#/components/schemas/Tiltakskode"
        kanKombineresMed:
          type: array
          items:
            type: string
      required:
        - sanityId
        - navn
        - kanKombineresMed

    VeilederflateTiltak:
      oneOf:
        - $ref: "#/components/schemas/VeilederflateTiltakGruppe"
        - $ref: "#/components/schemas/VeilederflateTiltakEnkeltplassAnskaffet"
        - $ref: "#/components/schemas/VeilederflateTiltakEgenRegi"
        - $ref: "#/components/schemas/VeilederflateTiltakEnkeltplass"

    VeilederflateTiltakGruppe:
      type: object
      properties:
        type:
          const: "TILTAK_GRUPPE"
        tiltakstype:
          $ref: "#/components/schemas/VeilederflateTiltakstype"
        tiltaksnummer:
          type: string
        status:
          $ref: "#/components/schemas/GjennomforingStatus"
        beskrivelse:
          type: string
        faneinnhold:
          $ref: "#/components/schemas/Faneinnhold"
        kontaktinfo:
          $ref: "#/components/schemas/VeilederflateKontaktInfo"
        oppstart:
          $ref: "#/components/schemas/GjennomforingOppstartstype"
        id:
          type: string
          format: uuid
        navn:
          type: string
        stedForGjennomforing:
          type: string
        apentForPamelding:
          type: boolean
        oppstartsdato:
          type: string
        sluttdato:
          type: string
        arrangor:
          $ref: "#/components/schemas/VeilderflateArrangor"
        fylker:
          type: array
          items:
            type: string
        estimertVentetid:
          $ref: "#/components/schemas/EstimertVentetid"
        personvernBekreftet:
          type: boolean
        personopplysningerSomKanBehandles:
          type: array
          items:
            $ref: "#/components/schemas/PersonopplysningData"
      required:
        - tiltakstype
        - status
        - kontaktinfo
        - oppstart
        - id
        - navn
        - apentForPamelding
        - oppstartsdato
        - arrangor
        - personvernBekreftet
        - personopplysningerSomKanBehandles
        - fylker

    VeilederflateTiltakEgenRegi:
      type: object
      properties:
        type:
          const: "TILTAK_EGEN_REGI"
        tiltakstype:
          $ref: "#/components/schemas/VeilederflateTiltakstype"
        tiltaksnummer:
          type: string
        status:
          $ref: "#/components/schemas/GjennomforingStatus"
        beskrivelse:
          type: string
        faneinnhold:
          $ref: "#/components/schemas/Faneinnhold"
        kontaktinfo:
          $ref: "#/components/schemas/VeilederflateKontaktInfo"
        oppstart:
          const: LOPENDE
        sanityId:
          type: string
        navn:
          type: string
        stedForGjennomforing:
          type: string
        fylker:
          type: array
          items:
            type: string
      required:
        - tiltakstype
        - status
        - kontaktinfo
        - oppstart
        - sanityId
        - navn
        - fylker

    VeilederflateTiltakEnkeltplass:
      type: object
      properties:
        type:
          const: "TILTAK_ENKELTPLASS"
        tiltakstype:
          $ref: "#/components/schemas/VeilederflateTiltakstype"
        status:
          $ref: "#/components/schemas/GjennomforingStatus"
        beskrivelse:
          type: string
        faneinnhold:
          $ref: "#/components/schemas/Faneinnhold"
        kontaktinfo:
          $ref: "#/components/schemas/VeilederflateKontaktInfo"
        oppstart:
          const: LOPENDE
        sanityId:
          type: string
        navn:
          type: string
        stedForGjennomforing:
          type: string
        fylker:
          type: array
          items:
            type: string
      required:
        - tiltakstype
        - status
        - kontaktinfo
        - oppstart
        - sanityId
        - navn
        - fylker

    VeilederflateTiltakEnkeltplassAnskaffet:
      type: object
      properties:
        type:
          const: "TILTAK_ENKELTPLASS_ANSKAFFET"
        tiltakstype:
          $ref: "#/components/schemas/VeilederflateTiltakstype"
        tiltaksnummer:
          type: string
        status:
          $ref: "#/components/schemas/GjennomforingStatus"
        beskrivelse:
          type: string
        faneinnhold:
          $ref: "#/components/schemas/Faneinnhold"
        kontaktinfo:
          $ref: "#/components/schemas/VeilederflateKontaktInfo"
        oppstart:
          const: LOPENDE
        sanityId:
          type: string
        navn:
          type: string
        stedForGjennomforing:
          type: string
        fylker:
          type: array
          items:
            type: string
        arrangor:
          $ref: "#/components/schemas/VeilderflateArrangor"
      required:
        - tiltakstype
        - status
        - kontaktinfo
        - oppstart
        - sanityId
        - navn
        - arrangor
        - fylker

    RegelverkLenke:
      type: object
      properties:
        _id:
          type: string
        regelverkUrl:
          type: string
        regelverkLenkeNavn:
          type: string
      required:
        - _id
        - regelverkUrl
        - regelverkLenkeNavn

    PortableTextTypedObject:
      type: object
      additionalProperties: true
      properties:
        _type:
          type: string
        _key:
          type: string
      required:
        - _type

    Faneinnhold:
      type: object
      properties:
        forHvemInfoboks:
          type: string
        forHvem:
          type: array
          items:
            $ref: "#/components/schemas/PortableTextTypedObject"
        detaljerOgInnholdInfoboks:
          type: string
        detaljerOgInnhold:
          type: array
          items:
            $ref: "#/components/schemas/PortableTextTypedObject"
        pameldingOgVarighetInfoboks:
          type: string
        pameldingOgVarighet:
          type: array
          items:
            $ref: "#/components/schemas/PortableTextTypedObject"
        kontaktinfo:
          type: array
          items:
            $ref: "#/components/schemas/PortableTextTypedObject"
        kontaktinfoInfoboks:
          type: string
        oppskrift:
          type: array
          items:
            $ref: "#/components/schemas/PortableTextTypedObject"
        lenker:
          type: array
          items:
            $ref: "#/components/schemas/Lenke"
        delMedBruker:
          type: string

    FaneinnholdRequest:
      type: object
      properties:
        forHvemInfoboks:
          type: [string, "null"]
        forHvem:
          type: object
        detaljerOgInnholdInfoboks:
          type: [string, "null"]
        detaljerOgInnhold:
          type: object
        pameldingOgVarighetInfoboks:
          type: [string, "null"]
        pameldingOgVarighet:
          type: object
        kontaktinfo:
          type: object
        kontaktinfoInfoboks:
          type: [string, "null"]
        delMedBruker:
          type: [string, "null"]

    VeilederflateKontaktInfo:
      type: object
      properties:
        tiltaksansvarlige:
          type: array
          items:
            $ref: "#/components/schemas/VeilederflateKontaktinfoTiltaksansvarlig"
      required:
        - tiltaksansvarlige

    VeilderflateArrangor:
      type: object
      properties:
        selskapsnavn:
          type: string
        organisasjonsnummer:
          type: string
        kontaktpersoner:
          type: array
          items:
            $ref: "#/components/schemas/VeilederflateArrangorKontaktperson"
      required:
        - selskapsnavn
        - kontaktpersoner

    VeilederflateArrangorKontaktperson:
      type: object
      properties:
        id:
          type: string
          format: uuid
        navn:
          type: string
        epost:
          type: string
        telefon:
          type: [string, "null"]
        beskrivelse:
          type: [string, "null"]
      required:
        - id
        - navn
        - epost
        - telefon
        - beskrivelse

    VeilederflateKontaktinfoTiltaksansvarlig:
      type: object
      properties:
        _id:
          type: string
        navn:
          type: string
        enhet:
          $ref: "#/components/schemas/NavEnhet"
        telefon:
          type: string
        epost:
          type: string
        beskrivelse:
          type: string
      required:
        - _id
        - navn
        - epost

    NavEnhetStatus:
      type: string
      enum:
        - UNDER_ETABLERING
        - AKTIV
        - UNDER_AVVIKLING
        - NEDLAGT

    NavEnhetType:
      type: string
      enum:
        - LOKAL
        - FYLKE
        - TILTAK
        - ALS
        - KO

    BrregVirksomhet:
      type: object
      properties:
        organisasjonsnummer:
          type: string
        navn:
          type: string
        organisasjonsform:
          type: string
      required:
        - organisasjonsnummer
        - organisasjonsform
        - navn

    Arrangor:
      type: object
      properties:
        id:
          type: string
          format: uuid
        organisasjonsnummer:
          type: string
        organisasjonsform:
          type: string
        navn:
          type: string
        overordnetEnhet:
          type: [string, "null"]
        underenheter:
          type: array
          items:
            $ref: "#/components/schemas/Arrangor"
        slettetDato:
          type: string
          format: date
      required:
        - id
        - organisasjonsnummer
        - organisasjonsform
        - navn
        - overordnetEnhet

    PaginertUserNotifications:
      type: object
      required:
        - pagination
        - data
      properties:
        pagination:
          $ref: "#/components/schemas/Pagination"
        data:
          type: array
          items:
            $ref: "#/components/schemas/UserNotification"

    UserNotification:
      type: object
      properties:
        id:
          type: string
          format: uuid
        type:
          $ref: "#/components/schemas/NotificationType"
        title:
          type: string
        description:
          type: [string, "null"]
        user:
          type: string
        createdAt:
          type: string
          format: date-time
        doneAt:
          type: string
          format: date-time
        metadata:
          anyOf:
            - type: ["null"]
            - type: object
              properties:
                link:
                  type: string
                linkText:
                  type: string
      required:
        - id
        - type
        - title
        - description
        - user
        - createdAt
        - doneAt

    NotificationType:
      type: string
      enum:
        - NOTIFICATION
        - TASK

    UserNotificationSummary:
      type: object
      properties:
        notDoneCount:
          type: integer
      required:
        - notDoneCount

    Oppgave:
      type: object
      properties:
        id:
          type: string
          format: uuid
        type:
          $ref: "#/components/schemas/OppgaveType"
        title:
          type: string
        description:
          type: string
        tiltakstype:
          $ref: "#/components/schemas/OppgaveTiltakstype"
        link:
          type: object
          properties:
            linkText:
              type: string
            link:
              type: string
          required:
            - linkText
            - link
        createdAt:
          type: string
          format: date-time
        oppgaveIcon:
          $ref: "#/components/schemas/OppgaveIcon"
      required:
        - type
        - title
        - tiltakstype
        - link
        - createdAt
        - oppgaveIcon

    OppgaveIcon:
      type: string
      enum:
        - TILSAGN
        - UTBETALING

    OppgaveTiltakstype:
      type: object
      properties:
        tiltakskode:
          $ref: "#/components/schemas/Tiltakskode"
        navn:
          type: string
      required:
        - tiltakskode
        - navn

    AvbrytGjennomforingAarsak:
      type: string
      enum:
        - ENDRING_HOS_ARRANGOR
        - BUDSJETT_HENSYN
        - FOR_FAA_DELTAKERE
        - FEILREGISTRERING
        - AVBRUTT_I_ARENA

    AvbrytGjennomforingRequest:
      type: object
      properties:
        aarsak:
          $ref: "#/components/schemas/AvbrytGjennomforingAarsak"
      required:
        - aarsak

    CustomAvbrytGjennomforingRequest:
      type: object
      properties:
        aarsak:
          type: string

    AvbrytAvtaleAarsak:
      type: string
      enum:
        - ENDRING_HOS_ARRANGOR
        - BUDSJETT_HENSYN
        - FEILREGISTRERING
        - AVBRUTT_I_ARENA

    AvbrytAvtaleRequest:
      type: object
      properties:
        aarsak:
          $ref: "#/components/schemas/AvbrytAvtaleAarsak"
      required:
        - aarsak

    CustomAvbrytAvtaleRequest:
      type: object
      properties:
        aarsak:
          type: string

    SetAvtaleForGjennomforingRequest:
      type: object
      properties:
        avtaleId:
          type: string
          format: UUID

    GjennomforingDeltakerSummary:
      type: object
      properties:
        antallDeltakere:
          type: number
        deltakereByStatus:
          type: array
          items:
            type: object
            properties:
              status:
                type: string
              count:
                type: integer
            required:
              - status
              - count
      required:
        - antallDeltakere
        - deltakereByStatus

    GjennomforingKontaktperson:
      type: object
      properties:
        navIdent:
          type: string
        navn:
          type: string
        epost:
          type: string
        mobilnummer:
          type: [string, "null"]
        navEnheter:
          type: array
          items:
            type: string
        beskrivelse:
          type: [string, "null"]
      required:
        - navn
        - navIdent
        - navEnheter
        - epost
        - mobilnummer
        - beskrivelse

    GjennomforingKontaktpersonRequest:
      type: object
      properties:
        navIdent:
          type: string
        navEnheter:
          type: array
          items:
            type: string
        beskrivelse:
          type: [string, "null"]
      required:
        - navIdent
        - navEnheter
        - beskrivelse

    Endringshistorikk:
      type: object
      properties:
        entries:
          type: array
          items:
            $ref: "#/components/schemas/EndringshistorikkEntry"
      required:
        - entries

    EndringshistorikkEntry:
      type: object
      properties:
        id:
          type: string
          format: uuid
        operation:
          type: string
        editedAt:
          type: string
          format: date-time
        editedBy:
          $ref: "#/components/schemas/EndringshistorikkUser"
      required:
        - id
        - operation
        - editedAt
        - editedBy

    EndringshistorikkUser:
      oneOf:
        - $ref: "#/components/schemas/EndringshistorikkSystembruker"
        - $ref: "#/components/schemas/EndringshistorikkNavAnsatt"

    EndringshistorikkSystembruker:
      type: object
      properties:
        navn:
          type: string
      required:
        - navn

    EndringshistorikkNavAnsatt:
      type: object
      properties:
        navIdent:
          type: string
        navn:
          type: string
      required:
        - navIdent
        - navn

    Toggles:
      type: string
      enum:
        - mulighetsrommet-veilederflate.vis-tilbakemelding
        - mulighetsrommet.tiltakstype.migrering.deltaker
        - mulighetsrommet.tiltakstype.migrering.okonomi
        - mulighetsrommet.tiltakstype.pamelding-info-komet
        - mulighetsrommet.admin-flate.deltakerliste

    PublisertRequest:
      type: object
      properties:
        publisert:
          type: boolean
      required:
        - publisert

    SetApentForPameldingRequest:
      type: object
      properties:
        apentForPamelding:
          type: boolean
      required:
        - apentForPamelding

    SetTilgjengligForArrangorRequest:
      type: object
      properties:
        tilgjengeligForArrangorDato:
          type: string
          format: date
      required:
        - tilgjengeligForArrangorDato

    SetStengtHosArrangorRequest:
      type: object
      properties:
        periodeStart:
          type: string
          format: date
        periodeSlutt:
          type: string
          format: date
        beskrivelse:
          type: string

    NavKontorStruktur:
      type: array
      items:
        type: object
        properties:
          region:
            $ref: "#/components/schemas/NavEnhet"
          kontorer:
            type: array
            items:
              $ref: "#/components/schemas/NavEnhet"
        required:
          - region
          - kontorer

    Oppskrift:
      type: object
      properties:
        _id:
          type: string
        navn:
          type: string
        beskrivelse:
          type: string
        _updatedAt:
          type: string
        steg:
          type: array
          items:
            $ref: "#/components/schemas/Steg"
      required:
        - _id
        - navn
        - beskrivelse
        - steg
        - _updatedAt

    Steg:
      type: object
      properties:
        navn:
          type: string
        innhold:
          type: array
          items:
            $ref: "#/components/schemas/PortableTextTypedObject"
        _type:
          type: string
        _key:
          type: string
      required:
        - navn
        - innhold
        - _type
        - _key

    VeilederJoyrideRequest:
      type: object
      properties:
        fullfort:
          type: boolean
        joyrideType:
          $ref: "#/components/schemas/JoyrideType"
      required:
        - fullfort
        - joyrideType

    JoyrideType:
      type: string
      enum:
        - OVERSIKT
        - DETALJER
        - HAR_VIST_OPPRETT_AVTALE

    EstimertVentetidEnhet:
      type: string
      enum:
        - uke
        - maned

    EstimertVentetid:
      type: object
      properties:
        verdi:
          type: number
        enhet:
          $ref: "#/components/schemas/EstimertVentetidEnhet"
      required:
        - verdi
        - enhet

    StengtPeriode:
      type: object
      properties:
        id:
          type: number
        start:
          type: string
          format: date
        slutt:
          type: string
          format: date
        beskrivelse:
          type: string
      required:
        - id
        - start
        - slutt
        - beskrivelse

    Lenke:
      type: object
      properties:
        lenkenavn:
          type: string
        lenke:
          type: string
          format: uri
        apneINyFane:
          type: boolean
        visKunForVeileder:
          type: boolean
      required:
        - lenkenavn
        - lenke
        - apneINyFane
        - visKunForVeileder

    Tiltakskode:
      type: string
      enum:
        - VARIG_TILRETTELAGT_ARBEID_SKJERMET
        - OPPFOLGING
        - JOBBKLUBB
        - GRUPPE_FAG_OG_YRKESOPPLAERING
        - GRUPPE_ARBEIDSMARKEDSOPPLAERING
        - DIGITALT_OPPFOLGINGSTILTAK
        - AVKLARING
        - ARBEIDSRETTET_REHABILITERING
        - ARBEIDSFORBEREDENDE_TRENING

    Personopplysning:
      type: string
      enum:
        - NAVN
        - KJONN
        - ADRESSE
        - TELEFONNUMMER
        - FOLKEREGISTER_IDENTIFIKATOR
        - FODSELSDATO
        - BEHOV_FOR_BISTAND_FRA_NAV
        - YTELSER_FRA_NAV
        - BILDE
        - EPOST
        - BRUKERNAVN
        - ARBEIDSERFARING_OG_VERV
        - SERTIFIKATER_OG_KURS
        - IP_ADRESSE
        - UTDANNING_OG_FAGBREV
        - PERSONLIGE_EGENSKAPER_OG_INTERESSER
        - SPRAKKUNNSKAP
        - ADFERD
        - SOSIALE_FORHOLD
        - HELSEOPPLYSNINGER
        - RELIGION
        - NASJONALITET
        - ADRESSESPERRE

    Kurstype:
      type: string
      enum:
        - BRANSJE_OG_YRKESRETTET
        - NORSKOPPLAERING
        - GRUNNLEGGENDE_FERDIGHETER
        - FORBEREDENDE_OPPLAERING_FOR_VOKSNE
        - STUDIESPESIALISERING

    AmoKategorisering:
      oneOf:
        - $ref: "#/components/schemas/BransjeOgYrkesrettet"
        - $ref: "#/components/schemas/Norskopplaering"
        - $ref: "#/components/schemas/ForberedendeOpplaeringForVoksne"
        - $ref: "#/components/schemas/GrunnleggendeFerdigheter"
        - $ref: "#/components/schemas/Studiespesialisering"

    BransjeOgYrkesrettet:
      type: object
      properties:
        kurstype:
          type: string
          enum:
            - BRANSJE_OG_YRKESRETTET
        bransje:
          $ref: "#/components/schemas/Bransje"
        forerkort:
          type: array
          items:
            $ref: "#/components/schemas/ForerkortKlasse"
        sertifiseringer:
          type: array
          items:
            $ref: "#/components/schemas/Sertifisering"
        innholdElementer:
          type: array
          items:
            $ref: "#/components/schemas/InnholdElement"
      required:
        - kurstype
        - bransje
        - innholdElementer
        - sertifiseringer
        - forerkort

    Norskopplaering:
      type: object
      properties:
        kurstype:
          type: string
          enum:
            - NORSKOPPLAERING
        norskprove:
          type: boolean
        innholdElementer:
          type: array
          items:
            $ref: "#/components/schemas/InnholdElement"
      required:
        - kurstype
        - norskprove
        - innholdElementer

    GrunnleggendeFerdigheter:
      type: object
      properties:
        kurstype:
          type: string
          enum:
            - GRUNNLEGGENDE_FERDIGHETER
        innholdElementer:
          type: array
          items:
            $ref: "#/components/schemas/InnholdElement"
      required:
        - kurstype
        - innholdElementer

    ForberedendeOpplaeringForVoksne:
      type: object
      properties:
        kurstype:
          type: string
          enum:
            - FORBEREDENDE_OPPLAERING_FOR_VOKSNE
      required:
        - kurstype

    Studiespesialisering:
      type: object
      properties:
        kurstype:
          type: string
          enum:
            - STUDIESPESIALISERING
      required:
        - kurstype

    Sertifisering:
      type: object
      properties:
        konseptId:
          type: number
        label:
          type: string
      required:
        - konseptId
        - label

    ForerkortKlasse:
      type: string
      enum:
        - A
        - A1
        - A2
        - AM
        - AM_147
        - B
        - B_78
        - BE
        - C
        - C1
        - C1E
        - CE
        - D
        - D1
        - D1E
        - DE
        - S
        - T

    Bransje:
      type: string
      enum:
        - INGENIOR_OG_IKT_FAG
        - HELSE_PLEIE_OG_OMSORG
        - BARNE_OG_UNGDOMSARBEID
        - KONTORARBEID
        - BUTIKK_OG_SALGSARBEID
        - BYGG_OG_ANLEGG
        - INDUSTRIARBEID
        - REISELIV_SERVERING_OG_TRANSPORT
        - SERVICEYRKER_OG_ANNET_ARBEID
        - ANDRE_BRANSJER

    InnholdElement:
      type: string
      enum:
        - GRUNNLEGGENDE_FERDIGHETER
        - JOBBSOKER_KOMPETANSE
        - TEORETISK_OPPLAERING
        - PRAKSIS
        - ARBEIDSMARKEDSKUNNSKAP
        - NORSKOPPLAERING

    KoblingerForKontaktperson:
      type: object
      properties:
        gjennomforinger:
          type: array
          items:
            $ref: "#/components/schemas/DokumentKoblingForKontaktperson"
        avtaler:
          type: array
          items:
            $ref: "#/components/schemas/DokumentKoblingForKontaktperson"
      required:
        - gjennomforinger
        - avtaler

    DokumentKoblingForKontaktperson:
      type: object
      properties:
        id:
          type: string
          format: uuid
        navn:
          type: string
      required:
        - id
        - navn

    FrikobleKontaktpersonRequest:
      type: object
      properties:
        kontaktpersonId:
          type: string
          format: uuid
        dokumentId:
          type: string
          format: uuid
      required:
        - kontaktpersonId
        - dokumentId

    PersonopplysningData:
      type: object
      properties:
        personopplysning:
          $ref: "#/components/schemas/Personopplysning"
        tittel:
          type: string
        hjelpetekst:
          type: [string, "null"]
      required:
        - personopplysning
        - tittel
        - hjelpetekst

    TiltakDeltMedBruker:
      type: object
      properties:
        navn:
          type: string
        tiltakstype:
          $ref: "#/components/schemas/TiltakDeltMedBrukerTiltakstype"
        createdAt:
          type: string
          format: date
        dialogId:
          type: string
        tiltakId:
          type: string
      required:
        - navn
        - tiltakstype
        - createdAt
        - dialogId
        - tiltakId

    TiltakDeltMedBrukerTiltakstype:
      type: object
      properties:
        tiltakskode:
          $ref: "#/components/schemas/Tiltakskode"
        arenakode:
          $ref: "#/components/schemas/TiltakskodeArena"
        navn:
          type: string
      required:
        - navn

    OpsjonsmodellData:
      type: object
      properties:
        opsjonMaksVarighet:
          type: [string, "null"]
          format: date
        opsjonsmodell:
          anyOf:
            - type: "null"
            - $ref: "#/components/schemas/OpsjonsmodellKey"
        customOpsjonsmodellNavn:
          type: [string, "null"]
      required:
        - opsjonMaksVarighet
        - opsjonsmodell
        - customOpsjonsmodellNavn

    OpsjonsmodellKey:
      type: string
      enum:
        - TO_PLUSS_EN
        - TO_PLUSS_EN_PLUSS_EN
        - TO_PLUSS_EN_PLUSS_EN_PLUSS_EN
        - ANNET
        - AVTALE_UTEN_OPSJONSMODELL
        - AVTALE_VALGFRI_SLUTTDATO

    OpsjonLoggRequest:
      type: object
      properties:
        avtaleId:
          type: string
          format: uuid
        nySluttdato:
          type: [string, "null"]
          format: date
        forrigeSluttdato:
          type: [string, "null"]
          format: date
        status:
          $ref: "#/components/schemas/OpsjonStatus"
      required:
        - avtaleId
        - nySluttdato
        - forrigeSluttdato
        - status

    OpsjonStatus:
      type: string
      enum:
        - OPSJON_UTLØST
        - SKAL_IKKE_UTLØSE_OPSJON
        - PÅGÅENDE_OPSJONSPROSESS

    OpsjonLoggRegistrert:
      type: object
      properties:
        id:
          type: string
          format: uuid
        aktivertDato:
          type: string
          format: date
        sluttDato:
          type: [string, "null"]
          format: date
        forrigeSluttdato:
          type: [string, "null"]
          format: date
        status:
          $ref: "#/components/schemas/OpsjonStatus"
      required:
        - id
        - aktivertDato
        - sluttDato
        - forrigeSluttdato
        - status

    SlettOpsjonLoggRequest:
      type: object
      properties:
        id:
          type: string
          format: uuid
        avtaleId:
          type: string
          format: uuid
      required:
        - id
        - avtaleId

    LagretFilterRequest:
      type: object
      properties:
        id:
          type: [string, "null"]
          format: uuid
        navn:
          type: string
        "type":
          $ref: "#/components/schemas/LagretDokumenttype"
        filter:
          type: object
        sortOrder:
          type: number
      required:
        - id
        - type
        - navn
        - filter
        - sortOrder

    LagretDokumenttype:
      type: string
      enum:
        - AVTALE
        - GJENNOMFORING
        - GJENNOMFORING_MODIA

    LagretFilter:
      type: object
      properties:
        id:
          type: string
          format: uuid
        brukerId:
          type: string
        navn:
          type: string
        "type":
          $ref: "#/components/schemas/LagretDokumenttype"
        filter:
          type: object
        sortOrder:
          type: number
      required:
        - id
        - brukerId
        - type
        - navn
        - filter
        - sortOrder

    SetNotificationStatusRequest:
      type: object
      properties:
        notifikasjoner:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
                format: uuid
              status:
                $ref: "#/components/schemas/NotificationStatus"
            required:
              - id
              - status

    Periode:
      type: object
      properties:
        start:
          type: string
          format: date
        slutt:
          type: string
          format: date
      required:
        - start
        - slutt

    TilsagnDefaultsRequest:
      type: object
      properties:
        gjennomforingId:
          schema:
            type: string
            format: uuid
        type:
          schema:
            $ref: "#/components/schemas/TilsagnType"
        prismodell:
          schema:
            $ref: "#/components/schemas/Prismodell"
        belop:
          schema:
            type: number
        kostnadssted:
          schema:
            type: string
        periodeStart:
          schema:
            type: string
            format: date
        periodeSlutt:
          schema:
            type: string
            format: date
      required:
        - gjennomforingId
        - type
        - prismodell
        - belop
        - kostnadssted
        - periodeStart
        - periodeSlutt

    TilsagnDefaults:
      type: object
      properties:
        id:
          type: string
          format: uuid
        gjennomforingId:
          type: string
          format: uuid
        type:
          $ref: "#/components/schemas/TilsagnType"
        periodeStart:
          type: string
          format: date
        periodeSlutt:
          type: string
          format: date
        kostnadssted:
          type: string
        antallPlasser:
          type: integer
        beregning:
          $ref: "#/components/schemas/TilsagnBeregningInput"

    TilsagnRequest:
      type: object
      properties:
        id:
          type: string
          format: uuid
        gjennomforingId:
          type: string
          format: uuid
        type:
          $ref: "#/components/schemas/TilsagnType"
        periodeStart:
          type: string
          format: date
        periodeSlutt:
          type: string
          format: date
        kostnadssted:
          type: string
        beregning:
          $ref: "#/components/schemas/TilsagnBeregningInput"
      required:
        - id
        - gjennomforingId
        - type
        - periodeStart
        - periodeSlutt
        - kostnadssted
        - beregning

    TilsagnType:
      type: string
      enum:
        - TILSAGN
        - EKSTRATILSAGN
        - INVESTERING

    TilsagnDto:
      type: object
      properties:
        id:
          type: string
          format: uuid
        type:
          $ref: "#/components/schemas/TilsagnType"
        periode:
          $ref: "#/components/schemas/Periode"
        belopGjenstaende:
          type: integer
        kostnadssted:
          $ref: "#/components/schemas/NavEnhet"
        beregning:
          $ref: "#/components/schemas/TilsagnBeregning"
        status:
          $ref: "#/components/schemas/TilsagnStatus"
        bestillingsnummer:
          type: string
      required:
        - id
        - type
        - periode
        - belopGjenstaende
        - kostnadssted
        - beregning
        - status
        - bestillingsnummer

    TilsagnDetaljerDto:
      type: object
      properties:
        tilsagn:
          $ref: "#/components/schemas/TilsagnDto"
        opprettelse:
          $ref: "#/components/schemas/TotrinnskontrollDto"
        annullering:
          $ref: "#/components/schemas/TotrinnskontrollDto"
        tilOppgjor:
          $ref: "#/components/schemas/TotrinnskontrollDto"
      required:
        - tilsagn
        - opprettelse

    TilsagnStatus:
      type: string
      enum:
        - TIL_GODKJENNING
        - GODKJENT
        - RETURNERT
        - TIL_ANNULLERING
        - ANNULLERT
        - TIL_OPPGJOR
        - OPPGJORT

    Besluttelse:
      type: string
      enum:
        - GODKJENT
        - AVVIST

    BesluttTilsagnRequest:
      oneOf:
        - $ref: "#/components/schemas/GodkjentTilsagnRequest"
        - $ref: "#/components/schemas/AvvistTilsagnRequest"

    GodkjentTilsagnRequest:
      type: object
      properties:
        besluttelse:
          $ref: "#/components/schemas/Besluttelse"
      required:
        - besluttelse

    AvvistTilsagnRequest:
      type: object
      properties:
        besluttelse:
          $ref: "#/components/schemas/Besluttelse"
        aarsaker:
          type: array
          items:
            $ref: "#/components/schemas/TilsagnAvvisningAarsak"
        forklaring:
          type: [string, "null"]
      required:
        - besluttelse
        - aarsaker
        - forklaring

    TilsagnTilAnnulleringRequest:
      type: object
      properties:
        aarsaker:
          type: array
          items:
            $ref: "#/components/schemas/TilsagnTilAnnulleringAarsak"
        forklaring:
          type: [string, "null"]
      required:
        - besluttelse
        - aarsaker

    TilsagnTilAnnulleringAarsak:
      type: string
      enum:
        - FEIL_REGISTRERING
        - GJENNOMFORING_AVBRYTES
        - ARRANGOR_HAR_IKKE_SENDT_KRAV
        - FEIL_ANNET

    TilsagnAvvisningAarsak:
      type: string
      enum:
        - FEIL_ANTALL_PLASSER
        - FEIL_KOSTNADSSTED
        - FEIL_PERIODE
        - FEIL_BELOP
        - FEIL_ANNET

    DelutbetalingReturnertAarsak:
      type: string
      enum:
        - FEIL_BELOP
        - FEIL_ANNET
        - AUTOMATISK_RETURNERT

    TilsagnBeregning:
      oneOf:
        - $ref: "#/components/schemas/TilsagnBeregningForhandsgodkjent"
        - $ref: "#/components/schemas/TilsagnBeregningFri"

    TilsagnBeregningInput:
      oneOf:
        - $ref: "#/components/schemas/TilsagnBeregningForhandsgodkjentInput"
        - $ref: "#/components/schemas/TilsagnBeregningFriInput"

    TilsagnBeregningOutput:
      oneOf:
        - $ref: "#/components/schemas/TilsagnBeregningForhandsgodkjentOutput"
        - $ref: "#/components/schemas/TilsagnBeregningFriOutput"

    TilsagnBeregningForhandsgodkjent:
      type: object
      properties:
        type:
          const: FORHANDSGODKJENT
        input:
          $ref: "#/components/schemas/TilsagnBeregningForhandsgodkjentInput"
        output:
          $ref: "#/components/schemas/TilsagnBeregningForhandsgodkjentOutput"
      required:
        - type
        - input
        - output

    TilsagnBeregningForhandsgodkjentInput:
      type: object
      properties:
        type:
          const: FORHANDSGODKJENT
        periode:
          $ref: "#/components/schemas/Periode"
        sats:
          type: integer
        antallPlasser:
          type: integer
      required:
        - type
        - periode
        - sats
        - antallPlasser

    TilsagnBeregningForhandsgodkjentOutput:
      type: object
      properties:
        type:
          const: FORHANDSGODKJENT
        belop:
          type: integer
      required:
        - type
        - belop

    TilsagnBeregningFri:
      type: object
      properties:
        type:
          const: FRI
        input:
          $ref: "#/components/schemas/TilsagnBeregningFriInput"
        output:
          $ref: "#/components/schemas/TilsagnBeregningFriOutput"
      required:
        - type
        - input
        - output

    TilsagnBeregningFriInput:
      type: object
      properties:
        type:
          const: FRI
        belop:
          type: integer
      required:
        - type
        - belop

    TilsagnBeregningFriOutput:
      type: object
      properties:
        type:
          const: FRI
        belop:
          type: integer
      required:
        - type
        - belop

    AvtaltSats:
      type: object
      properties:
        periodeStart:
          type: string
          format: date
        periodeSlutt:
          type: string
          format: date
        pris:
          type: integer
        valuta:
          type: string
      required:
        - periodeStart
        - periodeSlutt
        - pris
        - valuta

    ArrFlateUtbetalingStatus:
      type: string
      enum:
        - KLAR_FOR_GODKJENNING
        - BEHANDLES_AV_NAV
        - UTBETALT
        - VENTER_PA_ENDRING

    AdminUtbetalingStatus:
      type: string
      enum:
        - VENTER_PA_ARRANGOR
        - KLAR_TIL_BEHANDLING
        - RETURNERT
        - TIL_GODKJENNING
        - UTBETALT
        - OVERFORT_TIL_UTBETALING

    UtbetalingDetaljerDto:
      type: object
      properties:
        utbetaling:
          $ref: "#/components/schemas/UtbetalingDto"
        deltakere:
          type: array
          items:
            $ref: "#/components/schemas/DeltakerForKostnadsfordeling"
        linjer:
          type: array
          items:
            $ref: "#/components/schemas/UtbetalingLinje"
      required:
        - utbetaling
        - deltakere
        - linjer

    UtbetalingDto:
      type: object
      properties:
        id:
          type: string
          format: uuid
        status:
          $ref: "#/components/schemas/AdminUtbetalingStatus"
        periode:
          $ref: "#/components/schemas/Periode"
        beregning:
          $ref: "#/components/schemas/UtbetalingKompaktBeregning"
        godkjentAvArrangorTidspunkt:
          type: string
          format: date-time
        journalpostId:
          type: string
        createdAt:
          type: string
          format: date-time
        betalingsinformasjon:
          $ref: "#/components/schemas/Betalingsinformasjon"
        beskrivelse:
          type: [string, "null"]
        innsendtAv:
          type: string
        tilskuddstype:
          $ref: "#/components/schemas/Tilskuddstype"
      required: [id, status, periode, beregning, createdAt, tilskuddstype]

    DeltakerForKostnadsfordeling:
      type: object
      properties:
        id:
          type: string
          format: uuid
        navn:
          type: string
        foedselsdato:
          type: string
          format: date
        geografiskEnhet:
          type: string
        region:
          type: string
        status:
          $ref: "#/components/schemas/GruppetiltakDeltakerStatus"
        manedsverk:
          type: number
      required:
        - id
        - status
        - manedsverk
        - navn

    ArrFlateUtbetalingKompakt:
      type: object
      properties:
        id:
          type: string
          format: uuid
        status:
          $ref: "#/components/schemas/ArrFlateUtbetalingStatus"
        fristForGodkjenning:
          type: string
          format: date-time
        tiltakstype:
          $ref: "#/components/schemas/UtbetalingTiltakstype"
        gjennomforing:
          $ref: "#/components/schemas/UtbetalingGjennomforing"
        arrangor:
          $ref: "#/components/schemas/UtbetalingArrangor"
        periode:
          $ref: "#/components/schemas/Periode"
        belop:
          type: number
      required:
        - id
        - status
        - fristForGodkjenning
        - tiltakstype
        - gjennomforing
        - arrangor
        - periode
        - belop

    UtbetalingKompaktBeregning:
      type: object
      properties:
        belop:
          type: integer
      required:
        - belop

    ArrFlateUtbetaling:
      type: object
      properties:
        type:
          const: FORHANDSGODKJENT
        id:
          type: string
          format: uuid
        status:
          $ref: "#/components/schemas/ArrFlateUtbetalingStatus"
        fristForGodkjenning:
          type: string
          format: date-time
        tiltakstype:
          $ref: "#/components/schemas/UtbetalingTiltakstype"
        gjennomforing:
          $ref: "#/components/schemas/UtbetalingGjennomforing"
        arrangor:
          $ref: "#/components/schemas/UtbetalingArrangor"
        beregning:
          $ref: "#/components/schemas/ArrFlateBeregning"
        betalingsinformasjon:
          $ref: "#/components/schemas/Betalingsinformasjon"
        periode:
          $ref: "#/components/schemas/Periode"
      required:
        - type
        - id
        - status
        - fristForGodkjenning
        - tiltakstype
        - gjennomforing
        - arrangor
        - beregning
        - betalingsinformasjon
        - periode

    UtbetalingTiltakstype:
      type: object
      properties:
        navn:
          type: string
      required:
        - navn

    UtbetalingGjennomforing:
      type: object
      properties:
        id:
          type: string
          format: uuid
        navn:
          type: string
      required:
        - id
        - navn

    UtbetalingArrangor:
      type: object
      properties:
        id:
          type: string
          format: uuid
        organisasjonsnummer:
          type: string
        navn:
          type: string
        slettet:
          type: boolean
      required:
        - id
        - organisasjonsnummer
        - navn
        - slettet

    ArrFlateBeregningForhandsgodkjent:
      type: object
      properties:
        belop:
          type: integer
        antallManedsverk:
          type: number
        digest:
          type: string
        deltakelser:
          type: array
          items:
            $ref: "#/components/schemas/UtbetalingDeltakelse"
        type:
          const: FORHANDSGODKJENT
      required:
        - belop
        - antallManedsverk
        - digest
        - deltakelser
        - type

    ArrFlateBeregningFri:
      type: object
      properties:
        belop:
          type: integer
        type:
          const: FRI
        digest:
          type: string
      required:
        - belop
        - type
        - digest

    ArrFlateBeregning:
      type: object
      oneOf:
        - $ref: "#/components/schemas/ArrFlateBeregningForhandsgodkjent"
        - $ref: "#/components/schemas/ArrFlateBeregningFri"

    UtbetalingDeltakelse:
      type: object
      properties:
        id:
          type: string
          format: uuid
        startDato:
          type: string
          format: date
        sluttDato:
          type: string
          format: date
        forstePeriodeStartDato:
          type: string
          format: date
        sistePeriodeSluttDato:
          type: string
          format: date
        sistePeriodeDeltakelsesprosent:
          type: number
        manedsverk:
          type: number
        person:
          $ref: "#/components/schemas/UtbetalingDeltakelsePerson"
        veileder:
          type: string
      required:
        - id
        - startDato
        - forstePeriodeStartDato
        - sistePeriodeSluttDato
        - sistePeriodeDeltakelsesprosent
        - manedsverk
        - person

    UtbetalingDeltakelsePerson:
      type: object
      properties:
        navn:
          type: string
        fodselsdato:
          type: string
          format: date
        fodselsaar:
          type: integer
      required:
        - navn

    UtdanningslopDbo:
      type: object
      properties:
        utdanningsprogram:
          type: string
          format: uuid
        utdanninger:
          type: array
          items:
            type: string
            format: uuid
      required:
        - utdanningsprogram
        - utdanninger

    Utdanningslop:
      type: object
      properties:
        utdanningsprogram:
          $ref: "#/components/schemas/Utdanningsprogram"
        utdanninger:
          type: array
          items:
            $ref: "#/components/schemas/Utdanning"
      required:
        - utdanningsprogram
        - utdanninger

    UtdanningsprogramMedUtdanninger:
      type: object
      properties:
        utdanningsprogram:
          $ref: "#/components/schemas/Utdanningsprogram"
        utdanninger:
          type: array
          items:
            $ref: "#/components/schemas/Utdanning"
      required:
        - utdanningsprogram
        - utdanninger

    Utdanningsprogram:
      type: object
      properties:
        id:
          type: string
          format: uuid
        navn:
          type: string
      required:
        - id
        - navn

    Utdanning:
      type: object
      properties:
        id:
          type: string
          format: uuid
        navn:
          type: string
      required:
        - id
        - navn

    ArrangorflateTilsagn:
      type: object
      properties:
        id:
          type: string
          format: uuid
        gjennomforing:
          type: object
          properties:
            id:
              type: string
              format: uuid
            navn:
              type: string
          required: [navn, id]
        tiltakstype:
          type: object
          properties:
            navn:
              type: string
          required: [navn]
        type:
          $ref: "#/components/schemas/TilsagnType"
        periode:
          $ref: "#/components/schemas/Periode"
        status:
          $ref: "#/components/schemas/TilsagnStatusOgAarsaker"
        arrangor:
          type: object
          properties:
            id:
              type: string
              format: uuid
            organisasjonsnummer:
              type: string
            navn:
              type: string
          required: [id, organisasjonsnummer, navn]
        beregning:
          $ref: "#/components/schemas/TilsagnBeregning"
        gjenstaendeBelop:
          type: integer
        bestillingsnummer:
          type: string
      required: [periode, type, status, gjennomforing, tiltakstype, arrangor, beregning, id, gjenstaendeBelop, bestillingsnummer]

    Betalingsinformasjon:
      type: object
      properties:
        kontonummer:
          type: string
        kid:
          type: [string, "null"]
      required: [kontonummer, kid]

    GodkjennUtbetaling:
      type: object
      properties:
        digest:
          type: string
        betalingsinformasjon:
          $ref: "#/components/schemas/Betalingsinformasjon"
      required:
        - digest
        - betalingsinformasjon

    RelevanteForslag:
      type: object
      properties:
        deltakerId:
          type: string
          format: uuid
        antallRelevanteForslag:
          type: integer
      required:
        - deltakerId
        - antallRelevanteForslag

    TilsagnStatusOgAarsaker:
      type: object
      properties:
        status:
          $ref: "#/components/schemas/TilsagnStatus"
        aarsaker:
          type: array
          items:
            $ref: "#/components/schemas/TilsagnTilAnnulleringAarsak"

    GetOppgaverRequest:
      type: object
      properties:
        oppgavetyper:
          type: array
          items:
            $ref: "#/components/schemas/OppgaveType"
        tiltakskoder:
          type: array
          items:
            $ref: "#/components/schemas/Tiltakskode"
        regioner:
          type: array
          items:
            type: string
      required: [oppgavetyper, tiltakskoder, regioner]

    DelutbetalingStatus:
      type: string
      enum:
        - TIL_GODKJENNING
        - GODKJENT
        - RETURNERT
        - UTBETALT
        - OVERFORT_TIL_UTBETALING

    UtbetalingLinje:
      type: object
      properties:
        id:
          type: string
          format: uuid
        tilsagn:
          $ref: "#/components/schemas/TilsagnDto"
        status:
          $ref: "#/components/schemas/DelutbetalingStatus"
        belop:
          type: number
        gjorOppTilsagn:
          type: boolean
        opprettelse:
          $ref: "#/components/schemas/TotrinnskontrollDto"
      required:
        - id
        - tilsagn
        - belop
        - gjorOppTilsagn

    DelutbetalingRequest:
      type: object
      properties:
        id:
          type: string
          format: uuid
        tilsagnId:
          type: string
          format: uuid
        belop:
          type: number
        gjorOppTilsagn:
          type: boolean
      required: [id, tilsagnId, belop]

    OpprettDelutbetalingerRequest:
      type: object
      properties:
        utbetalingId:
          type: string
          format: uuid
        delutbetalinger:
          type: array
          items:
            $ref: "#/components/schemas/DelutbetalingRequest"
      required: [delutbetalinger, utbetalingId]

    Tilskuddstype:
      type: string
      enum:
        - TILTAK_DRIFTSTILSKUDD
        - TILTAK_INVESTERINGER

    ArrangorflateManuellUtbetalingRequest:
      type: object
      properties:
        tilskuddstype:
          $ref: "#/components/schemas/Tilskuddstype"
        gjennomforingId:
          type: string
          format: UUID
        periodeStart:
          type: string
        periodeSlutt:
          type: string
        beskrivelse:
          type: string
        kontonummer:
          type: string
        kidNummer:
          type: [string, "null"]
        belop:
          type: number
      required: [tilskuddstype, gjennomforingId, periodeStart, periodeSlutt, beskrivelse, kontonummer, kidNummer, belop]

    OpprettManuellUtbetalingRequest:
      type: object
      properties:
        gjennomforingId:
          type: string
          format: UUID
        periode:
          $ref: "#/components/schemas/Periode"
        beskrivelse:
          type: string
        kontonummer:
          type: string
        kidNummer:
          type: [string, "null"]
        belop:
          type: string
      required:
        - gjennomforingId
        - periodestart
        - periodeslutt
        - beskrivelse
        - kontonummer
        - belop

    BesluttDelutbetalingRequest:
      oneOf:
        - $ref: "#/components/schemas/GodkjentDelutbetalingRequest"
        - $ref: "#/components/schemas/AvvistDelutbetalingRequest"

    GodkjentDelutbetalingRequest:
      type: object
      properties:
        besluttelse:
          $ref: "#/components/schemas/Besluttelse"
      required: [besluttelse]

    AvvistDelutbetalingRequest:
      type: object
      properties:
        besluttelse:
          $ref: "#/components/schemas/Besluttelse"
        aarsaker:
          type: array
          items:
            $ref: "#/components/schemas/DelutbetalingReturnertAarsak"
        forklaring:
          type: [string, "null"]
      required: [besluttelse, aarsaker, forklaring]

    TotrinnskontrollDto:
      oneOf:
        - $ref: "#/components/schemas/TotrinnskontrollTilBeslutningDto"
        - $ref: "#/components/schemas/TotrinnskontrollBesluttetDto"

    TotrinnskontrollTilBeslutningDto:
      type: object
      properties:
        type:
          const: TIL_BESLUTNING
        behandletAv:
          $ref: "#/components/schemas/AgentDto"
        behandletTidspunkt:
          type: string
          format: date-time
        kanBesluttes:
          type: boolean
        aarsaker:
          type: array
          items:
            type: string
        forklaring:
          type: string
      required:
        - type
        - behandletAv
        - behandletTidspunkt
        - kanBesluttes
        - aarsaker

    TotrinnskontrollBesluttetDto:
      type: object
      properties:
        type:
          const: BESLUTTET
        behandletAv:
          $ref: "#/components/schemas/AgentDto"
        behandletTidspunkt:
          type: string
          format: date-time
        kanBesluttes:
          type: boolean
        aarsaker:
          type: array
          items:
            type: string
        forklaring:
          type: string
        besluttetAv:
          $ref: "#/components/schemas/AgentDto"
        besluttetTidspunkt:
          type: string
          format: date-time
        besluttelse:
          $ref: "#/components/schemas/Besluttelse"
      required:
        - type
        - behandletAv
        - behandletTidspunkt
        - aarsaker
        - kanBesluttes
        - besluttetAv
        - besluttetTidspunkt
        - besluttelse

    ArrangorflateGjennomforing:
      type: object
      properties:
        id:
          type: string
          format: uuid
        navn:
          type: string
        startDato:
          type: string
          format: date
        sluttDato:
          type: string
          format: date
      required: [id, navn, periode, startDato]

    AgentDto:
      oneOf:
        - $ref: "#/components/schemas/NavAnsattAgentDto"
        - $ref: "#/components/schemas/ArrangorAgentDto"
        - $ref: "#/components/schemas/SystemAgentDto"

    NavAnsattAgentDto:
      type: object
      properties:
        type:
          const: NAV_ANSATT
        navIdent:
          type: string
        navn:
          type: string
      required:
        - type
        - navIdent

    ArrangorAgentDto:
      type: object
      properties:
        type:
          const: ARRANGOR
      required:
        - type

    SystemAgentDto:
      type: object
      properties:
        type:
          const: SYSTEM
        navn:
          type: string
      required:
        - type
        - navn
