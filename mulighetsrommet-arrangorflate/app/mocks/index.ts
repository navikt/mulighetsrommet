export const configureMock = () => {
  // TODO Sett opp mocks korrekt
  // if (typeof document === "undefined") {
  //   configureMockServer().listen();
  // } else {
  //   configureMockWorker().start();
  // }
};
