{{#with title}}
    <h{{level}}{{#if blocks.length}} class="line_bottom"{{/if}}>{{text}}</h{{level}}>
{{/with}}
{{#each blocks}}
    <div class="block{{#unless @last}} block_middle{{/unless}}">
        {{> block-content/partials/block entries=this.entries }}
    </div>
{{/each}}
